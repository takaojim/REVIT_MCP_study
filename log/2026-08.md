# Event Log — 2026-08

> 事件流水帳，由 git hooks 和 AI Agent 共同維護。
> **嚴禁修改已有條目**，只能 append。
> 詳見 `log/README.md`。

## [2026-08-03 00:00] evaluation | session-summary 容量問題七月後重評

- actor: claude-sonnet-4-6 (via scheduled-task)
- files: log/2026-08.md
- trigger: scheduled（2026-04-27 用戶指定七月小聚後重評）
- summary: 依 2026-04-27 指令，於七月小聚結束後量化三項指標，給出啟動解法 A 的明確建議。

---

## 評估報告：session-summary 容量問題 — 2026-08-03 重評

### 背景

2026-04-27 觀察到 session-summary 條目在 6 天內累積 4 筆、平均 60–130 行，佔月度 log 71%，超出 CLAUDE.md Session Start Protocol（`-Tail 80`）的有效讀取窗口。使用者當時選擇暫緩，因「貢獻者只有 1 人」，並要求七月小聚後重評。

---

### 指標一：實際貢獻者數（協作摩擦觸發門檻）

```
git log --pretty=format:'%an' | sort -u
```

| 貢獻者 | 狀態 |
|--------|------|
| CHIANG SHUOTAO | 主維護者 |
| ChiuRay | 貢獻者 |
| CyberPotato0416 | 貢獻者 |
| Jacky820507 | 貢獻者 |
| ROY | 貢獻者 |
| Shuotao Chiang/江碩濤/CTCI-ATFSD | 主維護者（多帳號） |
| lesleyliuke | 貢獻者 |
| shuotao | 主維護者（多帳號） |

**實際獨立貢獻者：≥5 位**（去除主維護者同一人多帳號後）

🔴 **紅燈**：>1 人已觸發協作摩擦閾值，七月小聚確認已擴大貢獻者規模。

---

### 指標二：七月 log 總行數 + session-summary 占比

| 月份 | 總行數 | session-summary 條目 | 占用行數 | 占比 | 最大單條 |
|------|-------:|---------------------:|---------:|-----:|---------:|
| 2026-04 | 446 | 4 | 220 | 49% | 60 行 |
| 2026-05 | 651 | 6 | 428 | **66%** | **118 行** 🔴 |
| 2026-06 | 412 | 3 | 118 | 29% | 42 行 |
| **2026-07** | **188** | **0** | **0** | **0%** | **0 行** |

- 七月 log 188 行 < 800：🟢 **綠燈**
- 七月 session-summary 占比 0%：🟢 **綠燈**
- 七月最大單條 0 行：🟢 **綠燈**

**但請注意**：七月 0 筆 session-summary 並非「問題自癒」——而是七月的高密度 harvest/merge 工作未觸發 `/handoff`。七月全程以 `harvest`、`fork-audit`、`docs`、`fix` 等細粒度事件寫入，結構比以往更健康，但 `/handoff` 機制本身沒有行數護欄，下次使用仍有破窗風險。

---

### 指標三：歷史 session-summary 最大單條行數

| 月份 | 最大單條 | 閾值 100 行 | 判定 |
|------|:--------:|:-----------:|------|
| 2026-04 | 60 行 | ≤100 | 🟢 |
| 2026-05 | **118 行** | **>100** | 🔴 已破窗 |
| 2026-06 | 42 行 | ≤100 | 🟢 |
| 2026-07 | 0 行（無條目）| — | — |

🔴 **五月出現 118 行單條**，已超出 CLAUDE.md Session Start Protocol `-Tail 80` 窗口，意味著新 session 讀 log 時只能讀到該條目後段 80 行，前 38 行靜默遺失。

---

### 三個既有解法評估

| 解法 | 說明 | 代價 | 解決的問題 |
|------|------|------|------------|
| **A（立即）** | 改 `.claude/commands/handoff.md` 加 ≤40 行硬限制 | 5 分鐘 | **直接防止新條目膨脹** |
| **B（中期）** | archive 機制：舊 session-summary 搬至 `log/sessions-archive/YYYY-MM.md` | 數小時 | log 導覽 + 縮短主 log |
| **C（長期）** | 跨月持續性錨點：新月份首筆 append 上月末 session-summary stub | 數天 | 跨月 context 連續性 |

**三解法不互斥**，但 B/C 不能替代 A：
- B 只是把已有的長條目移走，無法防止未來產出長條目
- C 解決的是月份切換時的 context 斷層，與行數無關
- **只有 A 直接約束產出端**

---

### 綜合紅綠燈判定

| 指標 | 數值 | 門檻 | 判定 |
|------|------|------|------|
| a. 貢獻者數 | ≥5 位 | >1 = 有協作摩擦 | 🔴 紅 |
| b. 七月 log 行數 | 188 行 | >800 = 紅 | 🟢 綠 |
| b. 七月 session-summary 占比 | 0% | >70% = 紅 | 🟢 綠 |
| c. 歷史最大單條 | 118 行（2026-05） | >100 = 紅 | 🔴 紅（歷史已破） |
| c. 七月最大單條 | 0 行（無條目） | >100 = 紅 | 🟢 綠（因無條目） |

---

### 明確建議：**啟動解法 A**

**理由**：

1. **協作摩擦條件已成立**（8 個 git 帳號 = ≥5 位獨立貢獻者），2026-04-27 設定的觸發門檻「>1 人」已達成。
2. **五月已有實際破窗紀錄**（118 行），CLAUDE.md `-Tail 80` 無法讀完，表示機制有真實失效案例。
3. **七月 0 條目是偶然，不是治癒**。七月的大量活動以細粒度事件寫入，不代表下次複雜 session 不會觸發 `/handoff`。
4. **解法 A 只需修改 handoff.md 一個地方**，加入段落字數硬限制（例：每段 ≤8 行、總條目 ≤40 行），不需要任何基礎設施變更。

**不建議現在啟動 B 或 C**：七月 log 結構健康（188 行、全為細粒度事件）、無月份切換問題，B/C 的收益暫不足以抵銷投入成本。可在 B 年底（11 月小聚前）評估 B/C 的必要性。

**不建議繼續觀察**：指標 a 和歷史指標 c 都是紅燈，繼續等待只會讓下一個長 session-summary 在更多貢獻者環境中擴散問題。

---

### 解法 A 建議修改方向（供使用者參考，不直接動手）

在 `.claude/commands/handoff.md` 的格式模板後加入行數規範，例如：

```markdown
## 行數限制（新增）
- 每個段落（### 本 session 完成 / 對齊的概念 / Pending / 拒絕的選項 / 下次起手式）**最多 8 行**
- 整條 session-summary 條目（含標頭）**上限 40 行**
- 超出時：精簡 bullet、把細節 reference 到相關 domain 或 commit SHA，不在 summary 重複貼程式碼片段
```

---

*本條目由排程 AI session 自動產出（2026-04-27 指定觸發），非人工撰寫。*

## [2026-08-06 09:05] feat | get_mep_segments_and_sizes — MEP Segment/Size 領域盤點工具（唯讀）

- actor: claude-opus-5[1m] (via Claude Code, Windows)
- files: MCP/Core/Commands/CommandExecutor.MepSettings.cs, MCP/Core/CommandExecutor.cs, MCP-Server/src/tools/mep-tools.ts, CLAUDE.md, README.md, README.zh-TW.md, docs/DOCUMENT_AUDIENCE_INVENTORY.md, docs/BIM_MCP/reference/{architecture-v2,domain-index,personal-llm-wiki,philosophy-22-propositions,skills-index}.html, HANDOFF_2026-08-06_mep-settings-inventory-tool.md, log/2026-08.md
- trigger: manual（接 HANDOFF_2026-08-06 MEP 認證教學 session 的交接規格）
- summary: 實作 handoff 的 Tool 1（唯讀盤點），Tool 2（curate_mep_sizes 寫入）依規格只登記不實作、skill 經 TAO 裁決本次不做（skills 維持 51）。**API 先反射確認**：MetadataLoadContext 掃 RevitAPI 2026 與 2022 ref assembly，`MEPSize` / `Segment` / `PipeSegment` / `DuctSizeSettings`（indexer 吃 `DuctShape`）跨 R22–R26 介面完全一致，且 `PipeSegment` 是 `Segment` 唯一衍生類別（故管段掃描完備）。**兩層落地**：新 partial `CommandExecutor.MepSettings.cs`（FilteredElementCollector.OfClass(PipeSegment) → GetSizes()；`DuctSizeSettings.GetDuctSizeSettings(doc)[shape]` 走原生 `DuctSizeIterator`；全部 `UnitUtils.ConvertFromInternalUnits(..., Millimeters)`；零 Transaction）+ `CommandExecutor.cs` dispatch case；TS 定義放 `mep-tools.ts`（**非 handoff 建議的 base-tools.ts** — mep-tools 同時在 full 與 mep profile，歸屬更正確）。`get_` 前綴自動得到 readOnlyHint=true。**實測（Revit 2024 / Snowdon Towers Sample HVAC，R24 build 部署到 Addins\2024\RevitMCP\）**：16 管段 / 235 筆管尺寸 / 風管 Round 72+Rect 59+Oval 93；Copper-K 目錄含 handoff 指定的 6.35 / 25.4 / 152.4 / 304.8 mm，整組即英制序列換算，單位正確。**live 測試抓到兩個缺陷並修正**：① 風管的 inner/outer 三張表 224 筆全為固定佔位值 12 ft（3657.6 mm），輸出會誤導對帳 → 新增 `DescribeDuctSize` 只給 nominal + `DuctSizeNote` 說明；② 全量 dump 87,534 字元超過單次回傳上限 → 新增 `summaryOnly`（只回統計）與 `segmentName`（名稱篩選，給了它 includeDuct 預設轉 false）兩個參數，並為每段/每形狀補 `usedInSizeListsCount` / `usedInSizingCount`、回傳補 `SegmentTotalCount` 避免局部誤讀為全案。**red→green**：請老師在 Mechanical Settings 取消勾選 Copper-K 的 6.35 mm 一筆，重跑得 `usedInSizeLists/usedInSizing=false`、counts 15/16、`usedOnly=true` 正確濾掉該筆（reported 15/16）。**副帶發現（Revit 行為，非本工具所致）**：進出 Segments and Sizes 對話框按 OK 後，Copper-K 全 16 筆 inner diameter 被量化到最近的 1/32"（run1 為真實銅管 K 型 ID 如 0.305"/0.995"/5.741"，run2 全成 1/32 倍數如 0.3125"/1.0"/5.75"），outer 因本來就落在 1/32 格線上而未變 — 對 CNS 對帳與水力計算是實質資料劣化，已提醒老師該模型勿存檔。驗證：R26 + R24 build 皆 0 errors；`npm run build` 0 error；registerRevitTools() 168→**169**；10 處計數宣稱同步；`verify-qaqc.ps1 -SkipBuild -SkipDeploy` = **59 PASS / 0 FAIL / 0 WARN / 3 SKIP**。依專案慣例只 commit 本地、不 push。

## [2026-08-10 10:39] fix | issue #111/#99/#100/#110 收攏 + MEP size curation 落地

- actor: claude-opus-5[1m] (via Claude Code, Windows) — 4 支 sonnet subagent 並行執行，主 agent 驗證與整合
- files: MCP/Core/Commands/CommandExecutor.RoomFinish.cs, MCP/Core/CommandExecutor.cs, MCP-Server/src/tools/clash-tools.ts, domain/mep-opening-candidate-scan.md, domain/cad-block-point-placement.md, domain/tool-capability-boundary.md, MCP/Core/Commands/CommandExecutor.MepSizeCuration.cs, MCP/Core/Commands/CommandExecutor.MepSettings.cs, MCP-Server/src/tools/mep-tools.ts, MCP-Server/src/tools/annotations.ts, .claude/skills/mep-settings-curation/SKILL.md, domain/mep-mechanical-settings.md, CLAUDE.md, README.md, README.zh-TW.md, docs/*, scripts/verify-qaqc.ps1, log/2026-08.md
- trigger: manual（issue triage 後的批次收攏）
- summary: **#111**（bug）TS 註冊了 `sync_room_ceiling_finish_from_ceilings` / `remap_room_finish_codes` 但 `MCP/` 無 handler，呼叫必拋 `NotImplementedException` — 從已 fetch 的 fork remote `jacky/main` **單檔外科式移植** `CommandExecutor.RoomFinish.cs`（737 行，與來源 `diff` 逐字相同）+ dispatcher 2 個 case；刻意不 cherry-pick 原 commit（`d053839` 是 19 檔 11,402 行、`04b3477` 是 189,549 行含二進位）。相容性逐行核對後零調整：namespace/partial class 一致、ElementId 走 `RevitCompatibility.cs` 既有 helper、寫入都在 `Transaction` 內；`GetTypeMark(Element)` 與既有 `GetTypeMark(FamilySymbol)` 為合法多載。**#99** 依 @NicheSam 07-30 留言（id 5128684040）回填 `domain/mep-opening-candidate-scan.md` 全部 7 處 TODO（尺寸公式雙側 clearance、`suggestedOpeningSize` nested schema 統一 mm、7 條 `review_required` warningCodes、短交集 `<10mm`、**明確不設斜穿自動放行角度**、**v1 不給最小開孔尺寸常數**），並補上維護者 07-28 承諾的 `detect_clashes.csaSource.linkInstanceId` TS schema — C# `ClashDetector.cs:535` 早已讀取，語意（0 或省略走主模型）與 `mepSource.linkInstanceId` 相同。**#100** 依 @NicheSam 08-05 留言（id 5190268672）回填 `domain/cad-block-point-placement.md` 13 處 TODO 中的 10 處（transform 可信度四條件 + ≥3 anchor/殘差≤1mm、duplicate 10mm 且 existing/in_batch 分報、`OneLevelBased` only、兩輪實測數字），並寫入兩項 v1 政策裁決：**僅支援 Linked DWG**（Imported 留待後續，理由為實測已遇 `NotFound` 失連 + TotalTransform 語意風險）、**Create 可於明確核准後略過 duplicate**（附三條件：逐筆回傳原因與既有 ElementId／Preview 不得自行合併／核准須為明確參數）。餘 3 處 TODO 因 issue 留言未涵蓋而**保留未填、不編造**（Level 自動建立政策、mermaid 流程圖、duplicate/unsupported_family/transform 失敗實測案例）。**#110** 裁決**不納入核心**：pyRevit 按鈕無固定 `PostableCommandId` 的根因判斷正確，但方案 A 會把第三方執行引擎接進 bridge（含授權風險）、方案 B 反射對 pyRevit×Revit 雙版本脆弱且與 R22–R26 五 config 維護成本不成比例 — 於 `domain/tool-capability-boundary.md` 新增 L10 節記錄邊界與替代路徑（把邏輯收編成 MCP 工具直接測，而非驅動 UI）。同 commit 併入先前進行中的 **MEP size curation**（`curate_mep_sizes` 等三支工具 + skill + domain），因 `CommandExecutor.cs` 同時含兩線 case，拆開任一邊單獨 commit 皆無法 build。驗證：**Release.R22/R23/R24/R25/R26 五版 build 全數 0 errors**；`npm run build`（tsc + esbuild apps）0 error；`verify-qaqc.ps1 -SkipBuild -SkipDeploy` = **59 PASS / 0 FAIL / 0 WARN / 3 SKIP**；計數對齊 Tools 172 / Domain 76 / Skills 52。**未經 Revit runtime 實測** — #111 的兩支工具與 MEP curation 的寫入路徑均待實機驗證。依專案慣例只 commit 本地、不 push。

## [2026-08-10 10:48] fix | MCP Registry playbook 既有漂移修正（Current version 1.0.2 → 1.6.0）

- actor: claude-opus-5[1m] (via Claude Code, Windows) — mcp-registry-ops-inspect 稽核 → mcp-registry-sync 修正（皆 sonnet，依 CLAUDE.md 規定）
- files: docs/MCP_REGISTRY_PUBLISH.md, log/2026-08.md
- trigger: claude-hook（registry 一致性檢查觸發）
- summary: OPS 稽核發現 `docs/MCP_REGISTRY_PUBLISH.md` 的 `Current version` 停在 `1.0.2`，權威值（`server.json .version` / `.packages[0].version` / `MCP-Server/package.json .version` 三處平價）為 `1.6.0`。**屬既有漂移，非本 session 造成** —— 本次 commit 對 registry main files 的 diff 為空；成因是 `validate_publish_consistency.py` 只比對 `server.json` ↔ `package.json`、不檢查 playbook 文件，硬閘門長期綠燈蓋掉了這個問題。修正 3 處版本字樣（L21 fact 表、L140 `npm view` 範例輸出、L150 success verification 敘述），只向上對齊、未回退。**額外修正**：`Repo root`（L16）原為某台 Mac 的個人家目錄絕對路徑 `/Users/<OTHER_MACHINE_USER>/...`（已遮蔽），且同一路徑被複製進 L84（Path A）、L107（Path B）兩處可直接複製執行的 `cd` 指令 —— 已非環境註記而是會誤導的操作指引，任何人照抄都會 `No such file or directory`。改用專案既有慣例的 `<YOUR_PROJECT_PATH>` 佔位符（與 QAQC 3-4 對 config template 的要求一致），未改成另一台特定機器的路徑。未動 `server.json`／`MCP-Server/package.json`／`server.schema.json`／`publish-mcp.yml`；未執行任何 npm publish／mcp-publisher publish（發佈只走 `v*` tag）。驗證：`python scripts/validate_publish_consistency.py` **exit 0**（注意環境陷阱：本機 `python3` 指向 Windows Store stub，會回 exit 49 且無輸出，是假訊號，須用 `python`）。

## [2026-08-10 11:40] deploy | R24→Revit 2024、R26→Revit 2026 部署 + 發現 install-addon.ps1 相依漏複製缺陷

- actor: claude-opus-5[1m] (via Claude Code, Windows)
- files: （無 repo 檔案變更；本條為環境部署與缺陷發現紀錄）log/2026-08.md
- trigger: manual（#111 修正後需實機驗證）
- summary: 部署本次 `413f188` 建構產物到 Revit 2024（R24，13 個 DLL）與 Revit 2026（R26，8 個 DLL），逐檔 SHA256 驗證全數相符、addin manifest 一致。**未使用 `scripts/install-addon.ps1`**，因發現兩個缺陷：① **無法指定目標版本** —— L204-217 的 `do/while` 中 `$userVersion = $revitVersion` 恆為非 null，永不詢問使用者，白名單 `@("2026","2025","2024","2023","2022")` 由高到低取第一個存在的目錄；本機工作預設為 2024、2026 屬另一條作業線，直接執行會部署到錯誤版本。② **只複製 2 個 DLL**（L349 `RevitMCP.dll` + L376-385 `Newtonsoft.Json.dll`），其餘相依從不複製 —— 導致 **Revit 2026 部署長期缺 6 個相依**（`ClosedXML`／`DocumentFormat.OpenXml`／`ExcelNumberFormat`／`Irony`／`SixLabors.Fonts`／`XLParser`），Excel 相關工具（`import_excel_to_drafting_views`、`read_excel_tables`、`export_smoke_review_excel`、數量計算類）在 2026 上必然 runtime 拋 `FileNotFoundException`；且該處 DLL 停在 2026-08-03，不含 #111 修正。本次已補齊並更新。**兩代 runtime 相依差異（實測）**：R22/R23/R24 = 13 個 DLL（.NET Framework 4.8，含 `System.Buffers`／`System.IO.Packaging`／`System.Memory`／`System.Numerics.Vectors`／`System.Runtime.CompilerServices.Unsafe` 五個相容 shim）；R25/R26 = 8 個（.NET 8 由 runtime 內建）。**故部署驗證不得硬編「應有 N 個檔案」常數，必須以對應 config 的建構產物動態比對。** 另 `MCP/Core/RevitCompatibility.cs` 的 `#if REVIT2025_OR_GREATER` 使 `IdType` 在 int32／int64 間分歧，R24 與 R26 的 DLL **ABI 不相容**且錯版部署在複製階段無徵兆、只在 Revit 載入或呼叫時才爆 —— 防呆須做在工具內。`verify-qaqc.ps1` Phase 5（L375-414）目前僅列出安裝位置與偵測重複 addin，不查相依完整性／建構新鮮度／跨版本一致性，故上述 2026 缺件狀態 QAQC 全程綠燈未察覺。完整實作要求（R1-R8 + 5 條驗收，含兩條負向測試）已整理，待老師裁決先改腳本或先升級 QAQC Phase 5。備份：2024／2026 各留 `RevitMCP.dll.bak-20260810-*-pre-issue111`。

## [2026-08-10 12:20] feat | QAQC Phase 5 升級：部署完整性 / 新鮮度 / 跨版本一致性 / 殘留偵測（5-3~5-6 + -AddinsRoot）

- actor: claude-opus-5[1m] (via Claude Code, Windows) — Workflow 編排：fable(規劃) → sonnet(實作) → sonnet(觀察) → sonnet(唯讀稽核)，一輪通過，advisor 未啟動
- files: scripts/verify-qaqc.ps1, CLAUDE.md, log/2026-08.md
- trigger: manual（承接 R7；起因是部署 R24 時發現 Revit 2026 長期缺 6 個相依而 QAQC 全程綠燈）
- summary: Phase 5 原本只做 5-1（列出安裝位置）與 5-2（重複 addin 偵測），**查不出任何實質部署問題** —— Revit 2026 缺 6 個相依 DLL 的狀態跑幾次都是 PASS。本次新增四項檢查：**5-3 相依完整性**（已部署 DLL 集合須涵蓋對應 `MCP/bin/<config>/*.dll`，缺一即 FAIL 並指名檔案）、**5-4 建構新鮮度**（已部署 `RevitMCP.dll` 的 SHA256 對比建構產物，不符 WARN）、**5-5 跨版本一致性**（列出落後版本，WARN）、**5-6 殘留偵測**（`Addins\<year>\` 根層若有 `RevitMCP.dll` 即 WARN，屬 #91 之前的舊配置）。新增 **`-AddinsRoot`** 參數（預設 `$env:APPDATA\Autodesk\Revit\Addins`），目的是讓負向測試跑在暫存 fixture 上而非使用者的實際部署 —— 這是安全要求，不可移除。**硬性設計約束：不得硬編「應有 N 個檔案」常數** —— R22/R23/R24 為 13 個（.NET Framework 4.8 含 5 個相容 shim）、R25/R26 為 8 個（.NET 8 由 runtime 提供），兩者都正確，必須以對應 config 的建構產物動態列舉為基準。**驗收（主 agent 獨立複驗，非採信稽核者自述）**：正向 —— 5-3 對 2024 動態列 13、對 2026 列 8，證實未硬編；2022/2023 已裝 Revit 但未部署 → SKIP 而非 FAIL；5-6 正確抓到 2024 根層既有殘留。負向（獨立於 workflow 內部測試另跑一輪）—— 刪 2024 的 `XLParser.dll` → FAIL 並精準指名，exit 1；再刪 2026 的 `Irony.dll` + `SixLabors.Fonts.dll` → FAIL:2 且兩版本分別列出各自缺件；全部補回 → PASS，exit 0；fixture 已清除，真實部署未受影響（2024 仍 13 個、2026 仍 8 個）。回歸 —— `-SkipBuild -SkipDeploy` 維持 59 PASS / 0 FAIL / 0 WARN / 3 SKIP，與升級前基準一致。含 Phase 5 的完整跑分為 67 PASS / 0 FAIL / 1 WARN / 6 SKIP（該 WARN 即 2024 根層殘留，屬真實待清項目）。**負向測試是本次的關鍵驗收**：自動修改驗證工具本身有產出「永遠 PASS 假綠燈」的特有風險，因此驗收明文要求證明「該失敗時真的會失敗」，且 advisor 被禁止以移除負向測試的方式讓迴圈通過。Workflow 實測用量：4 支 agent / 379,286 tokens / 90 次工具呼叫 / 24 分鐘。

## [2026-08-10 12:36] feat | 新增 /loop-up skill（52→53）+ 修 QAQC 7-3 掃描路徑錯字（第三個假綠燈）

- actor: claude-opus-5[1m] (via Claude Code, Windows) — skill 草稿由 sonnet 背景產出，主 agent 驗證後落地
- files: .claude/skills/loop-up/SKILL.md, scripts/verify-qaqc.ps1, docs/BIM_MCP/shared.js, docs/BIM_MCP/index.html, docs/BIM_MCP/reference/skills-index.html, docs/BIM_MCP/reference/personal-llm-wiki.html, CLAUDE.md, README.md, README.zh-TW.md, docs/DOCUMENT_AUDIENCE_INVENTORY.md, log/2026-08.md
- trigger: manual
- summary: **(1) 修第三個假綠燈** —— `verify-qaqc.ps1` 的計數掃描清單寫的是 `docs\BIM_MCP\_shared.js`（底線開頭），實際檔案是 `docs/BIM_MCP/shared.js`（無底線），該檔**從未被 7-3 讀取過**，因此其中兩處過期的「50 個編排層 Skill」（L150 標題、L160 desc）長期未被發現，而 7-3 每次都報 PASS。修正路徑並把兩處一併訂正為 53。**負向測試證明修復有效**：刻意把 L160 改成 99 → 7-3 FAIL 並指名 `docs/BIM_MCP/shared.js:160 claims 99, truth is 53`（修正前不可能做到），測完已還原。**(2) 新增 `/loop-up` skill**（419 行）—— 多模型分工的自動迴圈修正流程：orchestrator(Fable) / implementer(Sonnet) / observer(Haiku, quote-only 契約) / inspector-ops(Fable 或 Opus, 唯讀) / advisor(Fable, 三次失敗後啟動)；含啟動前置硬門檻（四項全過才可開迴圈，單次成本量級約 1.3M tokens）、prompt-first 修正組裝、observer JSONL schema、三層終止條件與全域 advisor 額度安全閥、以及「修改驗證工具本身必須有負向測試」的護欄。skill 內明文標註既有的 `mcp-registry-sync`／`mcp-registry-ops-inspect` 依 CLAUDE.md 釘死 Sonnet、不在本配比調整範圍。計數 52→53 共同步 16 處（CLAUDE.md ×2、README ×1、README.zh-TW ×1、audience inventory ×1、index.html ×1、skills-index.html ×6、personal-llm-wiki.html ×2、shared.js ×2），並於 skills-index.html 補上 `/loop-up` 卡片與表格列（7-10 要求）。驗證：`verify-qaqc.ps1 -SkipBuild -SkipDeploy` = **59 PASS / 0 FAIL / 0 WARN / 3 SKIP**，Skills 真值 53。**本日累計發現三個同型假綠燈**（registry playbook 未被 validator 涵蓋、Phase 5 不查相依完整性、7-3 掃描路徑錯字），共通病徵為「檢查項存在、會跑、會報 PASS，但實際未驗到任何東西」，共通成因為只有正向測試、從未問過「該失敗時是否真的會失敗」。

## [2026-08-10 12:45] feat | /loop-up 加入 runs.jsonl 執行紀錄與第 5 次雙稽核校準輪

- actor: claude-opus-5[1m] (via Claude Code, Windows)
- files: .claude/skills/loop-up/SKILL.md, .claude/hooks/detect-loopup-calibration.sh, .claude/hooks/detect-registry-trigger.sh, .claude/settings.json, log/2026-08.md
- trigger: manual
- summary: 為 `/loop-up` 的模型配比表建立實證基礎。**設計上的根本問題**：配比表要回答「inspector 夠不夠好」，而 inspector 失效的定義是「它說 pass 但其實不 pass」—— 偵測這件事需要獨立於 inspector 的事實來源，但 human-out-of-the-loop 正好拿掉了那個來源。跑 100 次「inspector 說過了」，對於「inspector 會不會誤判」的資訊量是零。**解法**：`.claude/skills/loop-up/runs.jsonl` 每次執行結束 append 一筆（run_id / 各角色 model+tokens+tool_uses / attempts / advisor_invoked / final_pass / **failure_class**），每滿 5 筆下一次必須是**校準輪** —— 挑至少一個 Stage 派兩支不同模型的 inspector-ops 獨立稽核、互不可見，記錄**歧異**而非通過：兩支一致 = 弱證據支持現行配比（較弱者或可降級），兩支不同 = 有價值的樣本。歧異率是無人介入下唯一能逼近 inspector 可靠度的訊號。新增 `.claude/hooks/detect-loopup-calibration.sh`（PostToolUse，偵測 runs.jsonl 異動，`count % 5 == 0` 才注入提醒，否則安靜結束），註冊於 `.claude/settings.json`。**hook 實測**：第 4 筆不觸發、第 5 筆觸發，測試檔已清除。`failure_class` 列為必填（`spec_unclear` / `tech_pitfall` / `model_capability` / `criteria_contradiction`），因為**只有 `model_capability` 該用升級模型解決**，不記分類則第 5 次評估只會看到「失敗 N 次」而不知該調什麼。skill 中另明文記載目前配比表的證據狀態：首次實測（Phase 5 升級，4 支 agent / 379,286 tokens / 一輪通過）的 inspector 是 Sonnet，其結論與人工獨立複驗完全一致，故「inspector 應升級到 Fable/Opus」**目前僅為風險論證、尚無實證支持**，保留但需累積校準樣本後重新檢視。並誠實標註 n=5 且各次任務非同分布，只能給趨勢與異常訊號，不得生成看似精確的誤判率數字。skill 現 487 行（規範上限 500）。**順帶修正** `detect-registry-trigger.sh` 中的 `python3` → `python` 並加註警告：Windows 上 `python3` 可能指向 Store 的 App Execution Alias stub，回 exit 49 且無輸出，是假訊號（本 session 實際踩到）。

## [2026-08-10 14:08] feat | issue #98 Archicad 收編（skills 53→54）+ #74/#62 貢獻者成果寫入 domain 並具名

- actor: claude-opus-5[1m] (via Claude Code, Windows) — 4 支 sonnet 平行追溯 fork、1 支 sonnet 執行收編，主 agent 驗證與去重
- files: .claude/skills/archicad-skill-adapter/（SKILL.md + references/×4）, docs/integrations/×3, domain/tool-capability-boundary.md, domain/door-window-legend-workflow.md, domain/dwg-column-import.md, docs/BIM_MCP/reference/skills-index.html, docs/BIM_MCP/{index.html,shared.js}, docs/BIM_MCP/reference/personal-llm-wiki.html, CLAUDE.md, README.md, README.zh-TW.md, docs/DOCUMENT_AUDIENCE_INVENTORY.md, log/2026-08.md
- trigger: manual（追溯 #62/#74/#75/#98 四位貢獻者的 fork 進度並領回成果）
- summary: **四則追溯結果**（各派一支 sonnet 唯讀調查）：**#75** 排氣點 —— 4 個 fork 分支 + 9 筆上游 PR 逐一核對皆與主題無關，**無成果可領回**，agent 未代筆（僅留 `NOT_FOUND.md`）。**#74** 門窗圖例 —— fork 兩分支（07-02 純鏡像、景觀 heatmap）皆無關，作者唯一 PR #28 是三個半月前的無關主題，**無 fork 成果**；但 issue 內文的 A/B 架構分析品質足夠，經維護者裁決**不再等待原作者 PR，直接寫入 domain 並具名**。**#62** DWG 版本檢查 —— fork 內作者本人零 commit、唯一 PR #61 主題不符且開 103 秒自關；真正有價值的是**掛在 issue 上、從未被解壓縮讀過的 .docx 規格書**，首次解出全文後確認其版本判定依據是「連結外部檔的 `LastWriteTime` + 檔名版次 Regex」而**非** `AC1027`／`$ACADVER` 格式版本，觸發時機是**連結後稽核**而非匯入前掃描 —— 正好回答維護者三次追問（06-18／06-30／07-28）的兩個問題。**#98** Archicad —— fork `Archwiz-boss/BIM_MCP_study` 三分支確實存在，領回 9 份文件；發現 issue 未提及的 commit `998adce8`（Wave 2 提案 588 行），且 07-28 維護者回覆後零進展、從未提過上游 PR。**收編處置**：#98 經裁決全數收編 —— 新增 skill `archicad-skill-adapter`（+4 references）、`docs/integrations/` 三份文件、`domain/tool-capability-boundary.md` 新增 **L11**（fork 原編為 L10，會覆蓋今日剛加的 #110 邊界，故重編號；既有 L10 完全未動）。**skills 53→54，16 處計數同步**。**誠實標註**：三份 pilot 的 `Live-Test Evidence` 皆為空白模板、issue 宣稱的「已成功測試」只能證明 MCP 連線層級，故於 portability matrix 與 skills-index 卡片明文標示「尚未經 Skill 層級 live test 驗證」；matrix 基準為 52 skill、現為 54，未分級差集（`loop-up`／`mep-settings-curation`／`set-project-units`）與矩陣列有但目錄不存在的 `setup-archicad-mcp` 皆據實列出，**未代原作者補分級**（需 Archicad 領域判斷，無依據）。**貢獻者具名**：`door-window-legend-workflow.md` v1.5→1.6 新增〈架構取捨：圖例 Key 要承載在哪裡（A vs B）〉並於 frontmatter `contributors` 具名 @yunchen-kt；`dwg-column-import.md` v1.2→1.3 新增〈6.5 互補概念：連結後版本稽核〉並具名 @EricKuo123。**主 agent 攔下兩個問題**：① skills-index 出現**重複卡片**（主 agent 與 salvage agent 各加一張，QAQC 只驗「有沒有」不驗「有幾個」故仍 PASS）—— 已去重保留 salvage 版；② F 分類標題被誤加成 5（實際 4 張卡）—— 已修正，並順手訂正該頁既有錯字「速查：50 Skill 一覽」→ 54。驗證：`verify-qaqc.ps1 -SkipBuild -SkipDeploy` = **59 PASS / 0 FAIL / 0 WARN / 3 SKIP**，卡片總數 54 與 skill 真值一致。

## [2026-08-10 14:24] fix | QAQC 7-10 由「存在性」改為「剛好一張」+ 反向孤兒卡片偵測（第四個假綠燈）

- actor: claude-opus-5[1m] (via Claude Code, Windows)
- files: scripts/verify-qaqc.ps1, docs/BIM_MCP/reference/skills-index.html, log/2026-08.md
- trigger: manual（本日稍早實際踩到）
- summary: 7-10 原本只用 `-notmatch [regex]::Escape("/$s")` 判斷「skill 名稱有沒有出現在 skills-index.html」，**存在即 PASS**。今天實際發生：主 agent 與 salvage agent 各為 `archicad-skill-adapter` 加了一張卡片，兩張並存而 7-10 照樣 PASS —— 這是本日第四個同型假綠燈（前三：registry playbook 未被 validator 涵蓋、Phase 5 不查相依完整性、7-3 掃描路徑打成 `_shared.js` 導致該檔從未被讀取）。**改法**：改為計數而非測試 —— 每個 skill 必須有**剛好一張**卡片（`class="skill-name">/NAME</div>`）與**剛好一列**速查表（`<code>/NAME</code>`），0 張報 missing、≥2 張報 DUPLICATE；另新增**反向孤兒偵測**（掃出所有卡片名稱，若 `.claude/skills/<name>/SKILL.md` 不存在即報 orphan），可抓到 skill 更名/刪除後殘留的舊卡片 —— 這是舊的正向檢查在原理上看不到的。比對一律帶終結符（`</div>`、`</code>`）以避免前綴誤判（例如 `loop` 不會滿足 `loop-up` 的要求）。**新檢查立即抓到兩個既有缺漏**：`mep-settings-curation` 與 `set-project-units` 有卡片但**沒有速查表列**（先前的存在性檢查因卡片已滿足比對而看不到），已補上兩列。**負向測試（三情境）**：① 複製一張 `loop-up` 卡片 → FAIL `DUPLICATE card x2`；② 把卡片名改成不存在的 `ghost-skill` → FAIL 同時報 `loop-up : missing card` 與 `ghost-skill : orphan card`；③ 還原 → PASS。測試檔已還原。驗證：`verify-qaqc.ps1 -SkipBuild -SkipDeploy` = **59 PASS / 0 FAIL / 0 WARN / 3 SKIP**。**本日四個假綠燈的共通病徵**：檢查項存在、會跑、會報 PASS，但實際未驗到任何東西；共通成因是只有正向測試，從未問過「它該失敗的時候真的會失敗嗎」。

## [2026-08-10 14:32] verify | #111 兩支工具 Revit 2024 runtime 實測通過（bridge 已驗，核心演算法未觸發）

- actor: claude-opus-5[1m] (via Claude Code, Windows)
- files: log/2026-08.md（無程式碼變更；本條為實測紀錄）
- trigger: manual（承接 #111 修正後的實機驗證）
- summary: 環境 Revit 2024 / `Snowdon Towers Sample Architectural`，部署 DLL 的 SHA256 與 `MCP/bin/Release.R24/RevitMCP.dll` 一致（先確認 port 8964 可達、`get_project_info` + `get_active_view` 重新錨定，active view 為 `L2` CeilingPlan）。**① `sync_room_ceiling_finish_from_ceilings`（apply=false 預覽）**：讀到 L2 **7 間房 / 68 片天花板**，逐間回報 `MissingTargetParameter`（該模型無「天花板塗層」參數），附 `TypeMarkSource` 與逐間 `Warnings`。**② `remap_room_finish_codes`（dryRun）**：`TotalRooms: 7` 與 ① 一致，四個預設欄位皆不存在，`UnusedMappings: ["F11"]`，12ms。**③ 改用該模型確實存在的參數再測（仍 dryRun）**：`ChangedRooms: 2 / PlannedChanges: 2 / UnusedMappings: []`，正確命中 `201 Corridor` 與 `S3 Stair`、正確不動其餘 5 間，逐筆回傳 old→new —— **證明讀取／比對／規劃／回報整條路徑正確，而非僅「handler 有被呼叫」**。三次全部 `Applied: false`，模型零寫入。**結論**：#111 回報的 `NotImplementedException` 確定修復。**誠實邊界 —— 有一段未驗到**：兩支工具真正用途需台灣粉刷明細表的中文共用參數（樓板塗層／踢腳／牆面塗層／天花板塗層），Snowdon Towers 為 Autodesk 英文範例模型無此參數，故 `remap_room_finish_codes` 的 `+` 分隔多值與「F11→F10 但不動 F11 內的 F1」精確 token 比對、以及 `sync_room_ceiling_finish_from_ceilings` 的天花板↔房間重疊取樣（`sampleGrid`）與 `multiMatchStrategy` 多命中策略**皆未被觸發**。即 bridge 與 handler 已驗證、原作者實作的核心演算法尚未。已於 issue #111 留言載明此邊界並邀請 @Jacky820507 以含該組參數的實際專案補測。

## [2026-08-10 14:45] verify | MEP 四支工具 Revit 2024 runtime 實測（含 curate_mep_sizes 護欄；寫入路徑未跑）

- actor: claude-opus-5[1m] (via Claude Code, Windows) — 依 `mep-settings-curation` skill 流程
- files: log/2026-08.md（無程式碼變更；本條為實測紀錄）
- trigger: manual
- summary: 環境 Revit 2024 / **`Snowdon Towers Sample HVAC`**（專案編號 `7765328-33-M`）。**重新錨定抓到模型已切換** —— 上一輪 #111 實測時是 `Snowdon Towers Sample Architectural`，若沿用舊認知後續判斷全錯，此為 CLAUDE.md 每輪重新錨定規則的實例。**① `get_mep_segments_and_sizes`（summaryOnly）**：16 個管段 / 235 筆管尺寸；風管 Round 72 / Rectangular 59 / Oval 93；全部段的 `usedInSizeListsCount` 與 `usedInSizingCount` 皆等於 `sizeCount`。**② `get_mep_size_usage`（scope=duct）**：**目錄 ≠ 用量**的具體證據 —— Round 目錄 72 筆、實際只用 3 筆（101.6 / 152.4 / 203.2，最高 1421 次命中 / 997 元件）；Rectangular 目錄 59 筆、實際用 6 筆；**Oval 目錄 93 筆、用量 0**（整組 Revit 預設，本案未使用橢圓風管）。**Rectangular 發現 3 個 orphan**：120.65 / 222.25 / 234.95 mm（= 4.75" / 8.75" / 9.25"），`fromCurves: 0` 全來自配件，屬變徑配件帶出的中間尺寸，是「該增」候選而非錯誤。**③ `curate_mep_sizes` 擋下路徑（本次最重要）**：故意要求移除 Round 101.6 → 正確 `blocked`，回報 `usageCount: 1362` / `elementCount: 932` / `fromCurves: 502` / `fromFittings: 860` 並附 5 個元件 ID 樣本。該工具在 destructive allow-list 內，護欄失效即會刪掉 932 個元件在用的尺寸 —— 護欄有效且可歸因。**④ `curate_mep_sizes` 允許路徑**：dryRun 規劃移除 2 筆未使用的 Oval（3657.6 / 3632.2），`blocked: []`、`catalogCountBefore: 93`，並附完整 `RestorePayload`。**全程唯讀／dryRun，模型零寫入。** **順帶確認既有行為**：`RestorePayload` 中兩筆 Oval 的 `inner_mm`/`outer_mm` 皆為 `3657.6`（含公稱 3632.2 那筆），與 2026-08-06 記載的「Revit 對 duct 的 inner/outer 回傳固定佔位值 12 ft = 3657.6 mm」及工具 `DuctSizeNote` 一致，非缺陷；對 duct 無影響，但 pipe 的 RestorePayload 內外徑才是水力計算依據。**未驗到**：`dryRun: false` 的實際寫入未執行，故四步協定（列表→單一 Transaction→QC 重讀比對→誤刪自動還原）與 `Qc` 區塊尚未走到 —— 刻意保留，執行會真的變更使用者的 MEP 目錄設定。

## [2026-08-11 09:20] feat | 收編 PR #109 帷幕立面 + 修實測發現的三個缺陷（Tools 172→173）

- actor: claude-opus-5[1m] (via Claude Code, Windows) — 缺陷修正由 `/loop-up` 多模型迴圈執行（run `wf_eefd7224-69f`）
- files: MCP/Core/Commands/CommandExecutor.CurtainWallDimensions.cs（新增 4014 行 + 修正）, MCP/Core/Commands/CommandExecutor.CurtainWall.cs, MCP/Core/CommandExecutor.cs, MCP-Server/src/tools/curtain-wall-tools.ts, MCP-Server/src/tools/annotations.ts, domain/curtain-wall-elevation-workflow.md, CLAUDE.md, README.md, README.zh-TW.md, docs/DOCUMENT_AUDIENCE_INVENTORY.md, docs/BIM_MCP/reference/*.html, .claude/skills/loop-up/{SKILL.md,runs.jsonl}, log/2026-08.md
- trigger: manual（PR #109 salvage 收編）
- summary: **收編 PR #109**（@916kevin-gif，`優化與新增create_curtain_wall_elevations`）。依 fork PR 政策（`check-pr.yml` 只放行 `domain/` 與 `GEMINI.md`）走 salvage 路徑，比照 #86／#102。從 fork 分支整檔取用 4 檔（merge-base `907ef27`，main 未動過這 4 檔故無衝突）。**攔下一個回歸**：fork 版刪除了 `case "auto_dimension_walls"`（PR #102 才收編回來的工具），收編時只加不刪。**補上一個標註錯誤**：新工具 `diagnose_curtain_wall_elevation_dimensions` 會被 `diagnose_` 前綴判為 `readOnlyHint=true`，但它 `rollback:false` 時會把診斷尺寸留在模型裡，已於 `annotations.ts` 的 `OVERRIDE` 明確覆寫為寫入型。計數 172→173 同步 14 處，另修 QAQC pattern 未涵蓋的 3 處（README ×2 行文、`domain-index.html:77` 的「Skill 是編排（52 個）」→ 54）。**Revit 2024 實測（Snowdon Towers Sample Architectural，65 道帷幕牆）發現三個缺陷並修復**：① `verifyAssociationByMove` 在 pinned 網格線上必定失敗 —— 實測 `CurtainGridLineId: 946627` 的 `Type Association: Dependent`（類型排列規則產生，Revit 自動 pin），而 Dependent 是**預設情況**，故該驗證機制在多數真實模型上都跑不起來；修法為移動前解除 pin、測完**顯式還原**（不依賴 rollback 兜底，含還原失敗的錯誤記錄），並新增 `WasPinned`／`UnpinnedForTest` 回傳欄位。② 無批次範圍控制 —— 65 道牆一次做完超過 MCP 回應時限，**且 Revit 端其實跑完了、MCP 先 timeout**，使用者會誤判失敗而重跑並建出重複視圖；新增 `wallIds`／`maxWalls` 與 `RemainingWallIds`／`InvalidWallIds`／`Truncated` 回傳。③ 回應體積 —— dryRun 135,060 字元、diagnose 142,434 字元皆超單次回傳上限；比照 `get_mep_segments_and_sizes` 前例新增 `verbose`（預設 false）與 `OmittedDetailNote`。**Phase 2 Revit runtime 三項紅→綠實測通過**：S2 `TotalCurtainWalls` 65→**1** 且無 timeout；S3 回應 135K→約 1.1K、142K→約 1.5K，`verbose:true` 完整輸出一欄不少；**S1 `AssociationMoveTest.Passed` false→true**，`WasPinned:true`／`UnpinnedForTest:true`，網格線移動 10 mm 後段值 `[361.95,1219.2,361.95]`→`[371.95,1209.2,361.95]`（一段 +10、一段 −10、第三段不變）、`ReferencesRemainAvailable:true`、`StableRepresentationRoundTripPassed:true` —— **PR 的招牌宣稱「尺寸真的關聯 CurtainGridLine」至此才首次被證明**，先前只能證明「尺寸建出來了、Reference 取得了」。驗證：五版 build 0 errors、`npm run build` 0 error、`verify-qaqc.ps1 -SkipBuild -SkipDeploy` = 59 PASS / 0 FAIL / 0 WARN / 3 SKIP（主 agent 獨立複跑，非採信 inspector 自陳）。**迴圈越權事件**：S2 第 2 輪 implementer 為消除 QAQC 2-1 的 false positive 刪除範圍外的 `.agents/skills/hj-pr-proposal`（未被 git 追蹤，損害有限），inspector 看到卻未判 blocker —— 已記入 `runs.jsonl` 並補強 `loop-up` SKILL.md 的 inspector 強制條款：**範圍外改動若效果是讓檢查由 FAIL 變 PASS，一律判 blocker，看動機不看損害**。

## [2026-08-11 10:45] feat | QAQC 7-12 追蹤鏡像一致性 + .codex/ 納入 gitignore

- actor: claude-opus-5[1m] (via Claude Code, Windows)
- files: scripts/verify-qaqc.ps1, .gitignore, log/2026-08.md
- trigger: manual（調查工作區出現的來路不明檔案）
- summary: 工作區出現 45 個未追蹤的 `.agents/skills/*/SKILL.md` 鏡像與 `.codex/`，全部建於 2026-08-11 08:10:45 同一秒。查證：repo 內無任何腳本引用這兩個路徑（已搜 `*.ps1`／`*.sh`／`*.mjs`／`*.js`／`*.py`），`.codex/config.toml` 指向本專案的 `./MCP-Server/build/index.js`、`.codex/hooks/*.sh` 逐字複製自 `.claude/hooks/`（含前一日才建立的 `detect-loopup-calibration.sh`）。**經使用者確認：來源為其使用者端安裝的 OpenAI Codex 桌面 app，掃描專案後自行產生的本機工作檔。** **關鍵發現 —— `.agents/skills/` 是混合命名空間**：其中 8 個為 repo 內容（貢獻者 @Jacky820507 於 commit `604cafe` 提交時一併給的 `.agents` + `.claude` 雙鏡像，且 `docs/integrations/archicad-skill-portability.md:130` 將該鏡像列為 Archicad pilot 的驗證要件），其餘 45 個為 Codex 產生。已確認 Codex 未動到那 8 個追蹤檔。**新增 7-12 檢查**：追蹤中的 `.agents/skills/<name>/SKILL.md` 必須與 `.claude/skills/<name>/SKILL.md` 一致（比對前正規化行尾 —— 實測鏡像為 LF、來源為 CRLF，該差異非內容漂移），來源不存在則報 orphan。**範圍刻意限定為 `git ls-files`**：初版寫成掃檔案系統，實測對 Codex 的本機產物判 FAIL（53 個中 7 個過期，含 `loop-up` 鏡像停在 487 行、缺當日新增的 inspector 條款）—— 那會讓任何裝有此類工具的機器 QAQC 因與 repo 無關的原因變紅，違反此 gate 的職責，故修正並在註解明文禁止再擴大為檔案系統掃描。**`.codex/` 納入 `.gitignore`**：其 `hooks.json` 含 `C:\Users\<YOUR_USERNAME>\...` 硬編絕對路徑（`.claude/settings.json` 用的是 `$CLAUDE_PROJECT_DIR` 變數），`git add -A` 會將其吃入版控並在他人機器上全數失效。驗證：`verify-qaqc.ps1 -SkipBuild -SkipDeploy` = **60 PASS / 0 FAIL / 0 WARN / 3 SKIP**（7-12 為新增項，8 個追蹤鏡像全數一致）；`git check-ignore` 確認 `.codex/` 已被忽略、8 個追蹤鏡像未被誤擋。

## [2026-08-11 11:00] feat | install-addon.ps1 R1-R8：版本指定／全量複製／世代防呆／雜湊驗證／非互動／備份輪替／文件同步

- actor: claude-opus-5[1m] (via Claude Code, Windows)
- files: scripts/install-addon.ps1, domain/local-update-workflow.md, README.md, README.zh-TW.md, log/2026-08.md
- trigger: manual（承接 2026-08-10 部署時發現的腳本缺陷）
- summary: 依先前記錄的 R1–R8 需求改寫安裝腳本（428→493 行）。**R1 版本指定**：新增 `-Version`／`-All`，移除「白名單由高到低取第一個」的隱含預設（舊版 L204-217 的 `do/while` 中 `$userVersion = $revitVersion` 恆為非 null，**永遠不會真的詢問使用者**，等於總是挑最高版本；本機工作預設為 2024、2026 屬另一條作業線，直接執行會部署到錯誤版本）；未指定且多版本時，互動模式詢問、非互動模式**直接失敗而非猜測**。**R2 全量複製**：改為列舉 `MCP\bin\<config>\*.dll` 全部複製，取代舊版只複製 `RevitMCP.dll` + `Newtonsoft.Json.dll` 的硬編白名單 —— 該缺陷造成 Revit 2026 長期缺 6 個相依。**R3 世代防呆**：以相依組成判斷建構產物世代（R22–R24 應含 5 個 .NET Framework 4.8 相容 shim、R25–R26 不應含），不符即**中止**而非僅印警告；理由是 `RevitCompatibility.cs` 的 `IdType` 在 `Int32`／`Int64` 間依 `REVIT2025_OR_GREATER` 分歧，錯代部署在複製階段零徵兆、只在 Revit 載入時才爆。**R4 逐檔 SHA256 驗證**（含 manifest），不符即 FAIL 並列出檔名。**R5 非互動模式**：`-NonInteractive` 下不呼叫任何 `Read-Host`（全檔 16 處收斂為 `Wait-ForExit` 輔助函式），以 exit code 表達結果。**R6 備份輪替**：`-KeepBackups`（預設 3），舊備份自動清除。另新增 `-AddinsRoot` 供測試用暫存 fixture，理由同 `verify-qaqc.ps1` 的同名參數。**R8 文件同步**：`domain/local-update-workflow.md` v1.0→1.1 —— 移除「只有 `RevitMCP.dll` 需要被覆蓋與複製」這句**錯誤且造成實際損害**的指示（正是 Revit 2026 缺件的成因），改為一律使用腳本並說明相依數依世代不同（13 vs 8，兩者都對，故正確作法是複製整個產物目錄而非維護會過期的清單）；「Revit 目標版本 2025」標註為原始撰寫環境而非唯一支援版本、建構指令加上版本對應註解；README 兩份的部署段改用 `-Version` / `-All` 並說明腳本行為。**測試（暫存 fixture，未動實際部署）**：A 多版本未指定 + 非互動 → exit 1 正確擋下（舊版此情境會靜默部署到 2026）；B `-Version 2024` → 13 DLL、SHA256 全驗、2026 未被碰；C `-All` → 2024 = 13 DLL（.NET Framework 4.8）、2026 = 8 DLL（.NET 8），世代自動辨識；**D 負向測試 —— 暫時移走 R24 的 5 個 shim → exit 1 正確中止**並指名「應含 5 個、實際 0 個」，測後已還原（R24 仍 13 個 DLL）；E `-KeepBackups 2` 連跑 4 次 → `.bak` 剩 2 份。驗證：`verify-qaqc.ps1 -SkipBuild -SkipDeploy` = **60 PASS / 0 FAIL / 0 WARN / 3 SKIP**。R7（QAQC Phase 5 升級）已於 2026-08-10 完成，至此 R1–R8 全數落地。

## [2026-08-11 11:01] session-summary | issue/PR 全面收攏 + 五個假綠燈修復 + /loop-up 建立與首次實戰

- actor: claude-opus-5[1m] (via Claude Code, Windows)
- trigger: manual (/handoff)
- summary: 從「檢查 issue/PR 內容與合併策略」起手，收攏全部 open issue 與唯一 open PR，過程中連續發現五個「檢查會跑、會報 PASS、但實際什麼都沒驗到」的假綠燈並逐一修復（每個都做負向測試），期間建立 `/loop-up` 多模型迴圈 skill 並用它修 PR #109 的實測缺陷。

### 本 session 完成

- **#111 bug 修復並實測**（`413f188`）：TS 註冊了 `sync_room_ceiling_finish_from_ceilings` / `remap_room_finish_codes` 但 `MCP/` 無 handler。從已 fetch 的 fork remote `jacky/main` **單檔外科式移植**（737 行，與來源逐字相同），刻意不 cherry-pick 原 commit。Revit 2024 runtime 實測通過（`219d721`）。
- **五個假綠燈修復**：① registry playbook `Current version` 停在 1.0.2（`20d9dfe`）② QAQC Phase 5 不查相依完整性，導致 Revit 2026 缺 6 個 DLL 長期未被發現（`47f34e9`）③ 7-3 掃描路徑打成 `_shared.js`，該檔從未被讀取（`448ea12`）④ 7-10 只驗「有沒有卡片」不驗「有幾張」，兩支 agent 各加一張仍 PASS（`6d066c6`）⑤ 7-12 初版寫成掃檔案系統，會對本機 Codex 產物誤報 —— **上線前自己抓到並修正**（`5dce05a`）。
- **issue 收攏**：#99/#100 的 domain TODO 回填 + 兩項 v1 政策裁決；#110 範圍婉拒並寫入 `tool-capability-boundary.md` L10；#98 Archicad 全數收編（skills 52→54）；#74/#62 貢獻者成果具名寫入 domain（`d29e95b`）。open issue 8 → 5。
- **`/loop-up` skill 建立**（`448ea12`、`498f7be`）：多模型分工自動迴圈，含硬性啟動門檻、prompt-first 修正組裝、observer quote-only 契約、三層終止條件、runs.jsonl 與第 5 次雙稽核校準 hook。
- **PR #109 收編 + 三缺陷修正**（`e65015b`、`fdab5a3`）：攔下 `auto_dimension_walls` 回歸與 `diagnose_` 前綴的標註誤判；用 `/loop-up` 修 pinned 網格線測試、批次範圍控制、回應體積；Revit 2024 三項紅→綠實測通過。Tools 172→173。
- **`install-addon.ps1` R1–R8**（`b4384cb`）：版本指定、全量複製、世代防呆、SHA256 驗證、非互動、備份輪替、文件同步。fixture + 真實環境雙重驗證。
- QAQC 檢查數 59 → 60。R24/R26 部署完成（2026 原本缺 6 個相依）。

### 對齊的概念

- **「沒有報錯」≠「有在做事」**。本 session 的核心發現：五個假綠燈的共通病徵都是「檢查存在、會跑、報 PASS，但實際未驗到任何東西」，共通成因是**只有正向測試**，從沒問過「它該失敗的時候真的會失敗嗎」。一個永遠回「找不到資料」的工具，跟一個壞掉的工具，在測試輸出上看起來一樣。
- **負向測試是驗證的必要條件，不是加分項**。修改驗證工具本身時尤其如此 —— 迴圈有動機朝「讓自己通過」收斂，而最省事的收斂方式就是拿掉會失敗的檢查。此原則已寫成 `/loop-up` 不可被 advisor 移除的硬護欄。
- **看動機，不看損害**（loop-up inspector 條款）：範圍外改動若效果是「讓某個檢查從 FAIL 變 PASS」，一律判 blocker。來自實戰 —— S2 的 implementer 為消除 QAQC false positive 刪了範圍外目錄，inspector 看到卻因「損害有限」放行。
- **QAQC 的職責邊界是 repo，不是本機工具狀態**。7-12 初版對 Codex 產生的未追蹤鏡像判 FAIL，那會讓任何裝了該工具的機器因與 repo 無關的原因變紅。範圍限定 `git ls-files` 並在註解明文禁止再擴大。
- **測試模型一律不存檔 = 歸因的前提**（已存入專案記憶）。殘留會讓「這次的結果」無法歸因於「這次的改動」。
- **收編他人成果必須告知並具名**。#98/#74/#62/#109 都是單方面收編（fork PR 過不了 CI），四則都主動說明「沒有等你提 PR」與我們動過什麼，並在 domain frontmatter `contributors` 或 commit `Co-Authored-By` 具名。
- **skill 的 gate 必須真的套用**，否則是裝飾。`/loop-up` 建立後第一次遇到 #98 就判定不通過門檻（Archicad mapping 正確性無法在此環境自動驗證），第二次 #109 通過但誠實切成 Phase 1／2，第三次 install-addon 判定不值得（單檔、需求明確）。

### Pending（下 session 可接續）

- **#111 核心演算法未驗**：`+` 分隔多值、`F11`→`F10` 精確 token 比對、天花板重疊取樣，需要含台灣粉刷中文共用參數（`樓板塗層`/`踢腳`/`牆面塗層`/`天花板塗層`）的模型。已於 #111 留言邀請 @Jacky820507 補測。
- **#112 / #113 工具實作**：`scan_opening_candidates`、CAD 圖塊點位放置三支。規格已定案於對應 domain，可直接開工。
- **#98 / #74 / #75 球在貢獻者**：#98 等 PR-1、#74 邀請 review 已寫入的 A/B 節、#75 等 domain SOP。
- **Revit 2024 根層殘留 DLL** `Addins\2024\RevitMCP.dll`（#91 之前的舊配置，manifest 實際載入子夾那份）：7-12 與 install-addon 都會提醒，尚未刪除，待使用者裁決。
- **Codex 產生的 45 個未追蹤鏡像有 7 個過期**（含 `loop-up` 缺 inspector 條款）：使用者已表明不會用 Codex 跑 `/loop-up`，暫不處理。

### 拒絕的選項（省得重議）

- **#111 方向 3（「已知未實作」清單在註冊階段濾掉）**：那會讓「宣告與實作不一致」從 bug 降格成可長期共存的常態，等於承認工具清單可以說謊。
- **cherry-pick fork 的原 commit**：`d053839` 是 19 檔 11,402 行的合併提交、`04b3477` 是 189,549 行含二進位的 backup，會夾帶大量無關內容。改採單檔外科式移植。
- **刪除 Codex 產生的鏡像與 `.codex/`**：那是使用者本機工具的設定，刪掉等於毀掉別人的環境；且與剛寫下的「範圍外刪除一律 blocker」自相矛盾。改為 `.gitignore` 排除。
- **7-12 掃描檔案系統**：會對本機工具產物誤報，違反 QAQC 職責邊界。
- **對 #74 / #75 代寫 domain SOP**：#75 的局部高程極大值公式需要試車現場經驗，代筆會產出查無實據的工程假設；#74 雖經裁決直接寫入，但仍具名並保留作者 review 空間。
- **為 `install-addon.ps1` R1–R8 跑 `/loop-up`**：單檔、需求明確、可直接驗證，不符門檻第 3 項（多階段）。skill 自己就寫著「單一小改動不值得 5 角色的開銷」。

### 下次可接續的起手式

先跑 `.\scripts\verify-qaqc.ps1 -SkipBuild -SkipDeploy` 確認仍是 60 PASS / 0 FAIL，再依優先序擇一：① 若使用者手上有含中文粉刷參數的專案模型 → 補完 #111 的核心演算法驗證（唯一還掛著的「已修但未完整驗證」項目）；② 否則開工 #112（`scan_opening_candidates`，規格最完整、且前置的 `csaSource.linkInstanceId` schema 已補）。動 Revit 前記得：測試模型一律不存檔。
## [2026-08-17 00:10] feat | 1FL 房間牆心外側連續尺寸試作
- actor: gpt-5 (via Codex, Windows)
- files: scripts/trial_1fl_room_wall_center_dimensions.mjs, log/2026-08.md
- trigger: manual
- summary: 以正式 MCP 工具分析 1FL 房間與原生牆中心線，排除粉刷薄牆，依北南東西各最多三排建立總尺寸與連續牆心尺寸；共建立並驗證 24 道尺寸，未使用 DetailCurve 或 DetailLine 輔助線。

## [2026-08-21 00:35] feat | 柱間距標註納入 Dynamo 氣泡端點極值紅線演算法與全平面視圖批次標註

- actor: antigravity-agent (Windows)
- files: .claude/skills/auto-dimension/SKILL.md, .claude/skills/grid-column-dimension/SKILL.md, .agents/skills/auto-dimension/SKILL.md, .agents/skills/grid-column-dimension/SKILL.md, domain/grid-column-dimension-workflow.md, dynamo/標註-尺寸-柱列線.dyn, dynamo/README.md, scripts/batch_dimension_all_floorplans_dynamo.mjs, log/2026-08.md
- trigger: manual (使用者提供 Dynamo 參考腳本與紅線極值定義)
- summary: 將柱間距與柱心連續標註演算法對齊使用者 Dynamo 腳本 `標註-尺寸-柱列線.dyn`。四方極值定義為各視圖中軸線 2D 曲線端點極值（垂直軸線頂部氣泡端點基準線 maxY、底部氣泡端點基準線 minY、水平軸線左側氣泡端點基準線 minX、右側氣泡端點基準線 maxX）。標註位置依圖紙比例向建築物方向退縮偏移（外圈總長 500mm / 內圈細部 1150mm @ 1:100）。成功在 Revit 專案全部 8 個樓板平面圖視圖（1FL, 2FL, 3FL, 4FL, 5FL, GL, RFL, TRFL）批次建立共 64 條連續標註（每視圖 8 條），精確套用 TABC 專屬上右與下右型式，100% 原生 Grid 綁定。

## [2026-08-21 10:16] feat | 批次完成「立面圖(建築立面)」所有視圖柱間距雙層連續標註

- actor: antigravity-agent (Windows)
- files: scripts/batch_dimension_elevations.mjs, scripts/verify_elevation_dimensions.mjs, log/2026-08.md
- trigger: manual (使用者指示：進行"立面圖(建築立面)"所有視圖柱間距標註)
- summary: 自動識別專案中「立面圖 (建築立面)」分類下的 4 個建築立面視圖（北 ID:8157、東 ID:8176、南 ID:98984、西 ID:8237）。依各立面方位與軸線系統（南北向對應 A~H 軸線、東西向對應 1~8 軸線），自適應生成雙層連續標註：Tier 1 外圈總跨度尺寸（50.2m / 51.95m）與 Tier 2 內圈連續 7 區段細部柱間距標註。100% 採用純原生 Grid 軸線關聯（0 條 DetailCurve 輔助線），並依閱讀朝向套用專屬型式（北、東套用 `TABC-DIM_*/ S 2.5-柱心-上右`，南、西套用 `TABC-DIM_*/ S 2.5-柱心-下右`）。全數通過雙向驗證與區段數量確認。

## [2026-08-21 10:48] feat | 整合 Dynamo 樓層線幾何演算法：新增 auto_dimension_elevation_grids 工具並完成立面頂部柱心連續標註

- actor: antigravity-agent (Windows)
- files: MCP/Core/Commands/CommandExecutor.Dimension.cs, MCP/Core/CommandExecutor.cs, MCP-Server/src/tools/dimension-tools.ts, scripts/batch_auto_dimension_all_elevations.mjs, .agents/skills/auto-dimension/SKILL.md, .claude/skills/auto-dimension/SKILL.md, log/2026-08.md

## [2026-08-21 11:53] feat | 批次完成「立面圖(建築立面)」所有視圖全套標準雙層標註（頂部柱心朝下 + 側邊樓層朝右）

- actor: antigravity-agent (Windows)
- files: MCP/Core/Commands/CommandExecutor.Dimension.cs, scripts/batch_auto_dimension_all_elevation_master.mjs, log/2026-08.md
- trigger: manual (使用者指示：頂部柱間距尺寸標註方向也要像建築物朝下指向建物)
- summary: 修改 `auto_dimension_elevation_grids` 幾何方向為「由右至左」，使頂部柱間距雙層標註之 5mm 短輔助線全數**朝下（指向建築物內側）**；同時結合 `auto_dimension_elevation_levels`「由頂至底」，使側邊樓層高程雙層標註之 5mm 短輔助線全數**朝右（指向建築物內側）**。透過 `batch_auto_dimension_all_elevation_master.mjs` 在全部 4 個立面視圖（北 ID:8157、東 ID:8176、南 ID:98984、西 ID:8237）一鍵完成全套雙層標準標註（每視圖 4 條 Dimension，共 16 條），所有輔助線均完美指向建築物，文字與階梯間距完全符合出圖標準。

## [2026-08-21 12:30] feat | 批次完成「建地平面圖 (防火區劃)」全部 5 個樓層視圖四向雙層柱心連續標註並升級 10,000 筆查詢上限

- actor: antigravity-agent (Windows)
- files: MCP/Core/CommandExecutor.cs, scripts/batch_dimension_fire_plan_views.mjs, log/2026-08.md
- trigger: manual (使用者指示：3FL、4FL、5FL 為何沒有標註？我講所有視圖；並將 C# 預設上限提升至 10,000 筆)
- summary: 將 C# 核心 `QueryElements` 預設上限由 100 筆正式提升至 10,000 筆（<=0 時為 int.MaxValue），徹底解決建築大型專案圖元截斷問題。完整識別「建地平面圖 (防火區劃)」全部 5 個樓層視圖（1FL ID:395082、2FL ID:395092、3FL ID:1241890、4FL ID:1241900、5FL ID:2209908）。透過 `batch_dimension_fire_plan_views.mjs` 為全部 5 個視圖一鍵建立四向雙層柱心連續標註（每視圖 8 條，共 40 條 Dimensions），北/東套用 `上右`，南/西套用 `下右`，輔助線 100% 朝向建物內側。

## [2026-08-21 12:54] feat | 批次完成「剖面(建築剖面)」所有視圖頂部柱心與側邊樓層高程雙層標註

- actor: antigravity-agent (Windows)
- files: scripts/execute_section_dimensions.mjs, log/2026-08.md
- trigger: manual (使用者指示：請進行"剖面(建築剖面)"所有視圖。柱間距標示及樓層間距標示)
- summary: 自動識別專案中族群為「剖面」且類型為「建築剖面」的所有視圖（剖面 1 ID:590271、剖面 2 ID:624109、剖面 3 ID:624490）。一鍵批次建立頂部柱間距雙層標註（輔助線全數朝下指向建物）與側邊樓層距離雙層標註（輔助線全數朝右指向建物），各剖面均建立 4 條 Dimensions，3 個剖面共計 12 條標準連續標註。

## [2026-08-21 14:23] feat | 批次完成「樓板平面圖」全數 9 個視圖四向雙層柱心連續標註（適配當前專案軸線座標）

- actor: antigravity-agent (Windows)
- files: MCP/Core/Commands/CommandExecutor.Dimension.cs, MCP/Core/CommandExecutor.cs, scripts/execute_floor_plan_dimensions_correct.mjs, log/2026-08.md
- trigger: manual (使用者指示：沒有顯現，出了什麼問題；在執行一次)
- summary: 排查發現先前腳本座標採用前專案數值導致標註飛至畫布外。全面解析當前專案真實幾何（垂直軸線為 1-4 總跨 14,400mm、水平軸線為 A-D 總跨 15,900mm），精確設定北側（Y=4000/3350，1-4，上右）、南側（Y=-17500/-16850，1-4，下右）、西側（X=-1500/-850，A-D，下右）、東側（X=18000/17350，A-D，上右）之四向雙層連續標註，短輔助線 100% 朝向建物內側。為全部 9 個樓板平面圖建立共 72 條完美貼齊出圖規範的柱心標註。

## [2026-08-28 22:10] feat | 收編 PR #124 scan_opening_candidates（唯讀開孔候選掃描），Tools 176→177

- actor: claude-sonnet-5 (via Claude Code, Windows) — `/loop-up` implementer 角色，S6 收編階段
- files: MCP/Core/OpeningCandidateScanner.cs（新增）, MCP/Core/CommandExecutor.cs, MCP/Core/ClashDetector.cs, MCP-Server/src/tools/clash-tools.ts, MCP-Server/scripts/test-opening-candidate-contract.mjs（新增）, MCP-Server/package.json, domain/mep-opening-candidate-scan.md, CLAUDE.md, README.md, README.zh-TW.md, docs/DOCUMENT_AUDIENCE_INVENTORY.md, docs/BIM_MCP/index.html, docs/BIM_MCP/shared.js, docs/BIM_MCP/reference/{architecture-v2,domain-index,personal-llm-wiki,philosophy-22-propositions,skills-index,tools-index}.html, log/2026-08.md
- trigger: manual（`/loop-up` 多代理迴圈，orchestrator 核定後由本 implementer 執行）
- summary: **收編 @NicheSam 的 PR #124**（issue #112，head `b461f50d9876ef5db97309480791f9d0191f5033`，分支自 `bae94d9`）：`scan_opening_candidates` 唯讀 MEP 開孔候選掃描工具。PR 分支已落後主線多個 commit，其自身的 173→174 計數宣告已過期，故計數改由本次對照當前主線（176）自行推導為 177，不沿用 PR 內數字。**移植內容**：① `MCP/Core/OpeningCandidateScanner.cs`（新檔，412 行，逐字取自 PR）——把既有 `ClashDetector.DetectClashes` 的結果映射成唯讀候選契約，不另建第二套碰撞引擎；② `MCP/Core/CommandExecutor.cs` 新增 `case "scan_opening_candidates"` 派發與 `ScanOpeningCandidates` 私有 wrapper；③ `MCP/Core/ClashDetector.cs` 的**既有 bug 修復**——`CollectMepElements` 原本只在連結模型分支套用 `filters`，主模型來源會靜默忽略過濾條件；PR 把過濾區塊移到兩分支之外共用，修法已逐字核對與 PR diff 一致；④ `MCP-Server/src/tools/clash-tools.ts` 新增工具定義（`title`/`inputSchema`，`required: [mepSource, structureSource, clearanceMm]`）；⑤ `MCP-Server/scripts/test-opening-candidate-contract.mjs`（新增）+ `package.json` 的 `test:opening-candidate-contract` script。`domain/mep-opening-candidate-scan.md` 依 PR diff 同步狀態列（v1.0→v1.1）與實作對照段落，`contributors` frontmatter 已含 @NicheSam，未再重複新增。**計數同步 176→177**：CLAUDE.md 計數表、README.md／README.zh-TW.md（各 2 處）、DOCUMENT_AUDIENCE_INVENTORY.md，以及 `docs/BIM_MCP/{index,shared.js,reference/{architecture-v2,domain-index,personal-llm-wiki,philosophy-22-propositions,skills-index,tools-index}.html}` 全部字面 `176` 出處（含兩處因全形括號緊鄰導致 `sed \b176\b` 誤判無邊界而漏改，經 QAQC 7-1 精確報出檔名行號後補上）；`tools-index.html` 新增 `scan_opening_candidates` 卡片（D 類 16→17、唯讀計數 81→82、`八類加總等於 176→177` 等衍生數字一併同步）。**一處刻意偏離 PR 原文**：PR 在 domain 檔留下的 runtime smoke 記錄含一個可識別專案的模型檔名（依去識別化政策不在此複述）；依本 repo `domain/anti-lessons.md` 第 56 條「公開層一律去識別化」與 S6 任務的公開 repo硬限制，已在移植時**redact 檔名**，保留其餘可驗證數值（結構 link、clearanceMm、各項計數），並在文中註明係去識別化處理。**MCP Registry 連動**：`package.json` 新增 script 觸發 CLAUDE.md 的 registry 同步迴圈，已跑 `mcp-registry-sync`（判定：新增的是 `.scripts` 欄位，不屬版本/名稱/URL，無需修正）與 `mcp-registry-ops-inspect`（獨立複核，結論一致），`python scripts/validate_publish_consistency.py` exit 0（0 錯誤 0 警告），三處版本平價（1.6.0）與名稱皆未變動。
- **自行驗證（本次 session 實際跑過）**：`dotnet build -c Release.R24` 與 `-c Release.R26` 皆 0 errors；用暫時性、非分支的 detached-HEAD `git worktree`（建於 `C:\wt-360b349`，事後已 `git worktree remove`）建了 `360b349` 基準版本各跑一次同版本 build 做逐碼比對——`ClashDetector.cs`／`CommandExecutor.cs` 的每個 `(file, warning code)` 計數在改動前後**完全相同**（移動/新增的既有邏輯未產生新警告），新增警告全部落在新檔 `OpeningCandidateScanner.cs`：R24 為 CS0618×4／CS8600×8／CS8602×12／CS8603×2／CS8604×12／CS8620×2（共 40，含 R24 專屬的 `ElementId(int)` 過時建構子警告，R26 因 `IdType=long` 不觸發此項）、R26 為同組扣掉 CS0618 共 36，皆屬既有 nullable 分析告警風格、非新類別問題。`npm run build`（`tsc` + `build-apps.mjs`）通過；`npm run test:opening-candidate-contract` PASS；以 Node 直接呼叫 `registerRevitTools('full')` 確認 177 個工具、`scan_opening_candidates` 的 `title="Scan MEP Opening Candidates"`、`annotations.readOnlyHint=true`、`destructiveHint=false`（`scan_` 前綴本已在 `annotations.ts` 的 `READONLY_PREFIXES` 清單中，屬明確分類、非預設落空）；`verify-qaqc.ps1 -SkipBuild -SkipDeploy` = **64 PASS / 0 FAIL / 1 WARN / 3 SKIP**（唯一 WARN 為既有的 `check_sanitary_fixture_requirements` 隔離項，7-1/7-14/7-15/9-1 全數 PASS）；`git status --short --untracked-files=all` 僅列出上述意圖檔案，`git stash list` 為空。
- **僅採信、未複驗（來自 @NicheSam 回報，本機 Revit 服務未啟動、port 8964 未監聽，無法自行複測）**：2026-08-24 @NicheSam 於 Revit 2024 對其環境模型執行的 runtime smoke（模型檔名依上述去識別化政策不記錄）——MEP 主模型、結構 link `13291389`，`clearanceMm=25`、`maxCount=10`，回傳 `totalCandidates=10`、`candidateCount=10`、`reviewRequiredCount=0`、`failedCount=0`、`mepElementCount=1786`、`structureElementCount=3886`；另有負向測試：缺少 `clearanceMm` 時工具正確拒絕。此段資料完全來自對方回報，未經本次 session 在真實 Revit 環境重跑，記錄時明確標示為對方證據、不併入我方驗證結果。

## [2026-08-28 23:10] feat | 收編 PR #115 CAD 圖塊點位放置三工具（discover/preview/create），Tools 177→180

- actor: claude-sonnet-5 (via Claude Code, Windows) — `/loop-up` implementer 角色，S7 收編階段
- files: MCP/Core/CadBlockPlacementExecutor.cs（新增）, MCP/Core/CommandExecutor.cs, MCP-Server/src/tools/cad-block-placement-tools.ts（新增）, MCP-Server/src/tools/index.ts, domain/cad-block-point-placement.md, CLAUDE.md, README.md, README.zh-TW.md, docs/DOCUMENT_AUDIENCE_INVENTORY.md, docs/BIM_MCP/index.html, docs/BIM_MCP/shared.js, docs/BIM_MCP/reference/{architecture-v2,domain-index,personal-llm-wiki,philosophy-22-propositions,skills-index,tools-index}.html, log/2026-08.md
- trigger: manual（`/loop-up` 多代理迴圈，orchestrator 核定後由本 implementer 執行）
- summary: **收編 @Rumi-3653 的 PR #115**（issue #113，head `d69128a56eaad4431449e115a6a4022f56219713`，分支自 `bae94d9`）：`get_dwg_block_instances`（discover，唯讀）／`preview_family_instances_from_dwg_blocks`（preview，唯讀）／`create_family_instances_from_dwg_blocks`（create，寫入）三工具，實作 `domain/cad-block-point-placement.md` 規格（來源 issue #100，@NicheSam）。**PR #115 是 PR #114 的 reopen**——原帳號被 GitHub 標記，同一貢獻者換新帳號重新提交，非內容問題，已於 domain frontmatter 與內文註明。PR 分支已落後主線，其自身的 173→176 計數宣告已過期，計數改由本次對照當前主線（177）自行推導為 180，不沿用 PR 內數字。**移植內容**：① `MCP/Core/CadBlockPlacementExecutor.cs`（新檔，516 行，逐字取自 PR）——遍歷 Linked `ImportInstance` 幾何樹收集 Block（`GeometryInstance`）插入點與 transform、依 domain §3 判定 transform 可信度（finite／可逆／conformal／等比例，5% 容差）、依 domain §4.1 判定 duplicate（`duplicate_existing`／`duplicate_in_batch`）、依 domain §4.3 判定 `unsupported_family`（僅接受 `FamilyPlacementType.OneLevelBased`）、`create` 用主 `Transaction` + 逐筆 `SubTransaction` 建立並逐一 `verifiedExists` 回查；② `MCP/Core/CommandExecutor.cs` 新增三個 dispatcher case（純新增，比對現有樹逐行核對過插入點前後文，未刪動任何既有 case，插入位置在既有 `create_columns_from_dwg` case 之後、`CAD 連結模組` 之前）；③ `MCP-Server/src/tools/cad-block-placement-tools.ts`（新檔，逐字取自 PR）+ `MCP-Server/src/tools/index.ts` 掛載於 `full` 與 `mep` profile。`domain/cad-block-point-placement.md` version 0.2→0.3，`updated` 同步今日，`contributors`／`references` frontmatter 新增 @Rumi-3653／issue #113／PR #115（保留原 @NicheSam／issue #100 條目，未取代）。**計數同步 177→180**：CLAUDE.md 計數表、README.md／README.zh-TW.md（各 2 處）、DOCUMENT_AUDIENCE_INVENTORY.md，以及 `docs/BIM_MCP/{index,shared.js,reference/{architecture-v2,domain-index,personal-llm-wiki,philosophy-22-propositions,skills-index,tools-index}.html}` 全部字面 `177` 出處（逐檔 `grep -n "177"` 核對，含 `reference/external/karpathy-wiki-reading.html` 的 87 處確認皆為內嵌字型 base64／unicode-range 雜訊、與計數無關，未誤改）；`tools-index.html` 新增三張卡片（B 類 44→47、唯讀計數 82→84、會寫入計數 92→93、破壞性計數維持 3、來源模組 39→40、`八類加總等於 177→180` 等衍生數字一併同步、`data-tool` 與 runtime registry 雙向比對零缺漏零多餘）。
- **本階段的誠實要求（與 S6 不同）**：本 PR 是**寫入型工具、且從未被貢獻者本人在真實 Revit 執行過**（issue #113 明列 4 項未驗證，其中主 `Transaction` + 逐筆 `SubTransaction` 的「單筆失敗不回滾其他」是**必要驗收項**，因為規格來源 issue #100 2026-08-05 的實測通道本身未套用 SubTransaction）。已在 `domain/cad-block-point-placement.md` 開頭插入醒目狀態橫幅（v0.3），逐條列出四項未驗證內容（SubTransaction 部分失敗隔離／`TryGetBlockDisplayName` 的 blockName 來源可靠性／`OneLevelBased` offset 記帳／`unsupported_family`／`untrustworthy_transform`／duplicate 三種負向路徑），並明文禁止在回答或 log 中宣稱「已測試」「已驗證」。TS／C# 的工具描述本身維持乾淨、不夾帶免責聲明——狀態由 domain 檔案承載，依 `CLAUDE.md` Domain Method Compliance，任何 Agent 執行本流程前本就必須先讀這份 domain 檔。
- **自行驗證（本次 session 實際跑過，含 inspector-ops 覆核後補做的 R22／R23／R25）**：`dotnet build` 對 `Release.R22`／`Release.R23`／`Release.R24`／`Release.R25`／`Release.R26` **五組全跑，皆 0 errors**——R22／R23 起初本 session 只記了 R24／R26，經 inspector-ops 指出證據鏈缺口（其五組全過的說法先前來自貢獻者 PR 端未收編的 log、非本 session 證據）後，本 session 補上 R22／R23／R25 三組直接重建，不採信第二手數字。baseline 用 `git archive 816455a` 匯出到 scratch（未用 worktree）建了同版本基準各跑一次做逐碼比對——R22：baseline 1838 次出現，改動後 1868（+30，13 組合）；R23：baseline 1838，改動後 1868（+30，13 組合，與 R22 完全同組——R22/R23 共用 .NET Framework 4.8 + `IdType=Int32` 且 API 版本皆早於 `ElementId(int)` 被標記 Obsolete 的那一版，故新增警告不含 CS0618）；R24：baseline 660 個相異 (file,code,msg) 組合／1974 次出現，改動後 674 組合／2009 次出現，新增 14 組合、35 次出現（CS8600×14／CS8618×6／CS8604×6／CS0618×5／CS8602×3／CS8603×1；CS0618 是 `new ElementId(int)` 於 Revit 2024+ API 被標記 Obsolete 後的既有專案慣例，其餘 24 個檔案已有 130 次同類警告，非新問題）；R25：baseline 1967，改動後 1999（+32，13 組合，CS8600×15／CS8604×8／CS8618×6／CS8602×3，`IdType=Int64` 不觸發 CS0618）；R26：baseline 617／1958，改動後 630／1990，新增 13 組合／32 次出現，同組模式。**五組皆確認**：新增警告**全部**落在新檔 `MCP/Core/CadBlockPlacementExecutor.cs`，其他檔案的警告零新增、零消失、零數量變化。`npm run build`（`tsc` + `build-apps.mjs`）通過；`npm run test:opening-candidate-contract` PASS（既有測試未受影響）；以 Node 直接呼叫 `registerRevitTools('full')` 確認 180 個工具、零重名；`get_dwg_block_instances`／`preview_family_instances_from_dwg_blocks` 的 `readOnlyHint=true`（`get_`／`preview_` 前綴本已在 `annotations.ts` 的 `READONLY_PREFIXES`）、`create_family_instances_from_dwg_blocks` 的 `readOnlyHint=false`（`create_` 前綴落在 `WRITE_PREFIXES`，未被誤判唯讀）、三者 `destructiveHint` 皆為 `false`——`create_family_instances_from_dwg_blocks` 未被列進 `DESTRUCTIVE_NAMES` 允許清單（`delete_element`／`dedup_detail_elements_in_view`／`curate_mep_sizes`），且不需要列入：它只新增元素、不刪除或取代既有內容，與同語意的既有 `create_columns_from_dwg`／`create_beams_from_dwg` 分類一致，未動 `annotations.ts`。`verify-qaqc.ps1 -SkipBuild -SkipDeploy` = **64 PASS / 0 FAIL / 1 WARN / 3 SKIP**（唯一 WARN 為既有的 `check_sanitary_fixture_requirements` 隔離項，與本次改動無關；7-1／7-14／7-15／9-1 全數 PASS）；`git status --short --untracked-files=all` 僅列出上述意圖檔案（15 個修改＋2 個新增）、`git stash list` 為空、`git worktree list` 僅主 worktree。
- **仍未驗證（本次未做、且本來就不該由本 session 代勞）**：貢獻者與本 session 均**未對真實 Revit 執行過**這三支工具——上述橫幅列出的四項（SubTransaction 部分失敗隔離、blockName 來源可靠性、offset 記帳、三種負向路徑的真實觸發案例）全部待使用者在有 Linked DWG 的測試模型（不存檔）上實測後補上。

## [2026-08-29 00:39] docs | 收編 PR #122 archicad element-query pilot live-test evidence（純文件，計數不變）

- actor: claude-sonnet-5 (via Claude Code, Windows) — `/loop-up` implementer 角色，S9 收編階段
- files: .claude/skills/archicad-skill-adapter/references/pilot-element-query.md
- trigger: manual（`/loop-up` 多代理迴圈，orchestrator 核定後由本 implementer 執行）
- summary: **收編 @Archwiz-boss 的 PR #122**（issue #98，head `b7c00aacf87bd69feb6773c88a80d665329d849a`，分支自 `bae94d9`，fork 名稱 `BIM_MCP_study` 與本專案 `REVIT_MCP_study` 不同屬已知命名差異）：在 `pilot-element-query.md` 既有模板後補三小節（`Recorded run 2026-08-18`／`Follow-up verification 2026-08-19`／`Runtime findings` 七項），純文件、僅此一檔、未動 `domain/`、未新增 `.claude/skills/*/SKILL.md`，Domain（79）／Skills（54）計數皆不受影響，未動 `MCP/` 或 `MCP-Server/`。**先核對貢獻者自我更正**：PR 原始版本曾把「discovery 只是字面子字串比對」與「分頁 session 逾時」兩項當成 pilot 條文需修訂的理由；貢獻者於 issue #98 2026-08-18T16:06:58Z 留言公開收回該判斷（因兩者是 pinned `tapir-archicad-mcp==0.4.3`——已落後 `0.5.0`／`0.5.1`／`0.5.2`／`0.5.3` 四版——的版本特有行為，且 `GetDetailsOfElements` 431 個 pydantic 驗證錯誤早已是上游 `SzamosiMate/tapir-archicad-MCP#24` 既有 issue，非新發現），並聲明「PR #122 的檔案內容與描述已經修正」。**逐字核對本次 fetch 到的分支內容**，確認三項發現在檔案內文已標註為 `(0.4.3, may be fixed upstream)`／`(0.4.3, unverified on 0.5.x)`／明列上游 issue 連結，且明文「this finding should be re-tested before any pilot text is changed on account of it」——與留言的收回一致，未見任何仍主張「應據此改 pilot 條文」的殘留字句。**可歸屬性與去識別化檢查**：evidence 內的 `project: AC28 空白樣板20260709 (solo)`／`project_port: 19723` 經檢視為貢獻者本機的空白測試樣板檔（非真實客戶專案名稱或案號），屬合法技術證據非識別資訊，予以保留；GUID／版本號／property GUID 等數值性證據亦保留。**單位雙層落差發現未被轉成可執行規則**：本 PR 只新增到 `pilot-element-query.md` 的「Runtime findings」第 3 項（prose），PR 描述明言「這個 PR 不改規範條文」，未觸及 `.claude/skills/archicad-skill-adapter/references/revit-archicad-terminology.md` 第 72 行（現況僅一行 guardrail「Never apply Revit unit conversion automatically」，無可執行規則、未提兩層落差、未提 mm 換算前置條件）——已核實此行至今未變，維護者裁決留待後續 PR，本次未逾越 PR #122 範圍代為修改。
- **建議但未執行（留給維護者後續裁決，非本次 PR 範圍）**：在 `revit-archicad-terminology.md` 第 72 行 guardrail 補一條可執行規則，草案：「Before converting or persisting any Archicad property-layer numeric string (e.g. length, angle, area) into a Domain that computes in mm: (1) call `project_get_calculation_units` in this turn and record the returned unit/decimals; (2) never `parseFloat` a property-layer string without that unit context — the same wall can return `"18.96"` or `"1896"` with no error; (3) prefer the geometry-layer raw SI value when a numeric conversion is required, converting from metres/radians explicitly.」——僅整理自貢獻者已記錄的實測發現（`GetPropertyValuesOfElements` 顯示字串 vs 幾何層 SI 數值兩層落差、`project_get_calculation_units` 回傳範例），未新增任何未經貢獻者記錄的 Archicad API 行為推測。
- **驗收（本次 session 實際跑過）**：`git diff --stat bae94d9 FETCH_HEAD` 與套用後 `git diff --stat HEAD` 皆為單檔 127 行新增；`git apply --check` 乾跑通過後正式套用；`verify-qaqc.ps1 -SkipBuild -SkipDeploy` = **64 PASS / 0 FAIL / 1 WARN / 3 SKIP**（與收編前基準一致，唯一 WARN 為既有的 `check_sanitary_fixture_requirements` 隔離項，與本次改動無關）；`git status --short --untracked-files=all` 僅列出上述單一意圖檔案、`git stash list` 為空、`git worktree list` 僅主 worktree；未觸發 MCP Registry 同步迴圈（`server.json`／`package.json`／schema 三處皆未變動）。
- **僅採信、未複驗（來自 @Archwiz-boss 回報，本環境無 Archicad、無法連線 Tapir MCP、無法重跑任何指令）**：`tapir-archicad-mcp==0.4.3` 全部 discovery／pagination／property／geometry 讀值行為（304 個 Wall GUID、分頁 exhausted 細節、`Wall_ReferenceLineLength`/`Wall_CenterLength` 的 property GUID、`project_get_calculation_units` 回傳的 `Meter`/2 位小數/`DecimalDegree`、`"90°"` 與 `1.570796327` 的雙層對照、431 個 pydantic 驗證錯誤、`elements_highlight_elements` 空陣列清除行為）——本 session 未安裝 Archicad、未啟動 Tapir Add-On、未連線任何 Archicad MCP 埠，這些數字與行為完全來自對方回報，記錄時明確標示為對方證據、不併入我方驗證結果，亦未在文件中使用「已驗證」「已測試」字樣描述任何 Archicad-runtime 行為。

## [2026-08-29 01:02] docs | 收編 PR #123 archicad adapter 知識層 + 以 0.5.3 重評 Wave 2（純文件，計數不變）

- actor: claude-sonnet-5 (via Claude Code, Windows) — `/loop-up` implementer 角色，S10 收編階段
- files: .claude/skills/archicad-skill-adapter/SKILL.md, .claude/skills/archicad-skill-adapter/references/archicad-runtime-facts.md（新增）, .claude/skills/archicad-skill-adapter/references/revit-archicad-terminology.md, docs/integrations/archicad-skill-translation-wave2.md
- trigger: manual（`/loop-up` 多代理迴圈，orchestrator 核定後由本 implementer 執行）
- summary: **收編 @Archwiz-boss 的 PR #123**（issue #98，head `28d20a7a34bcca674c40ba13448d7b27c6ccdbf7`，分支 `docs/archicad-runtime-facts-and-units`，fork `Archwiz-boss/BIM_MCP_study`，分支自 `bae94d9`，已是本 repo 歷史祖先）。四檔純文件，逐檔行數：`SKILL.md` +4/-1、`archicad-runtime-facts.md` 新增 +235/-0、`revit-archicad-terminology.md` +77/-5、`archicad-skill-translation-wave2.md` +75/-0，合計 391 insertions(+) / 6 deletions(-)，與 `git diff --stat bae94d9 FETCH_HEAD` 一致。全部落在 `.claude/skills/archicad-skill-adapter/` 與 `docs/integrations/` 之下，未新增 `.claude/skills/*/SKILL.md`、未新增 `domain/*.md`、未動 `MCP/` 或 `MCP-Server/`——Domain（79）／Skills（54）／Tools（180）計數均不受影響。**補上 PR #122 遺留的單位缺口**：`revit-archicad-terminology.md` 新增「Units and the numeric contract」一節，取代原第 72 行僅有的一行 guardrail「Never apply Revit unit conversion automatically」。實際新增的可執行規則（逐字）：「- **Read the project's calculation units before converting anything.** The same wall returns `"18.96"` in a metre project and `"1896"` in a centimetre one, with no error and no warning. - **Do not parse a property string as a bare number.** It can carry a unit symbol, and its decimal places come from a project setting rather than from the value. - **Do not mix the two layers in one calculation.** A length from the property layer and a thickness from the geometry layer are not necessarily in the same unit. - **Revit-side Domains carry Revit-side units.** The takeoff Domains in `domain/` compute in millimetres. Feeding an Archicad metre value into one of those formulas is a factor-of-1000 error that every downstream total will inherit without complaint. - **Archicad working units are a third setting again.** They govern the drafting interface, no command to read them was found, and nothing observed here follows them.」——五條，非僅重述問題，且明確點名本 repo `domain/` 以 mm 計算會產生的 1000 倍誤差，正是 PR #122 log 中「建議但未執行」草案所要求的內容，本次由貢獻者自己補齊，未由本 session 代寫。**Wave 2 §6.4 修正有證據支撐**：`archicad-skill-translation-wave2.md` 新增第 0 節「2026-08-19 實測重評」，逐字保留原章節（PR 自述「原始內容未刪改」，已核對屬實），附重評結果表（逐字）：「| 原判斷 | 重測結果 | |---|---| | `wall-orientation-check` 只能走 capability-gap 人工檢查 | **前提不成立，建議升為 pilot**，見下方 (a) | | `detect-clashes` 縮限為 GUID pairs + 兩個 boolean | **維持**，schema 已確認，見下方 (b) | | Hotlink 不納入第一版 | **維持**，且已取得反面證據，見下方 (c) | | `element-coloring` 走暫時 Highlight | **維持**，但新增單位相關風險，見下方 (d) |」。§6.4 修正的依據是 `elements_get_details_of_elements` 的驗證錯誤訊息本身列出被拒欄位——`archicad-runtime-facts.md` 記載 0.5.3 新接受 `flipped`、`floorPlanPolygons`（0.4.3 拒絕），Tapir 1.5.8 新一批仍拒絕的欄位（逐字）：`referenceLineLocation`、`profileType`、`slantAlpha`、`slantBeta`、`topOffset`、`relativeTopStory`、`zoneRel`、`visibility`、`isAutoOnStoryVisibility`、`referenceMaterial`、`oppositeMaterial`、`sideMaterial`、`cutFillPen`、`cutFillBackgroundPen`——推論鏈是「驗證錯誤只會列出實際存在於回應中、但被 schema 拒絕的欄位」，故資料存在、只是 wrapper 吞掉，非 Archicad API 缺口；文件同時記載 Room-side 判斷改走 `elements_get_relations_of_elements` 的 `wallParts`（7 個 Zone 中 6 個回傳完整關係、1 個回傳空結構，文件明文「An empty result must be reported as distinct from a Zone that genuinely has no boundaries」），§6.4 建議「改寫方向」仍保留原「不自動宣稱 correct／incorrect」的驗收條件，屬有證據支撐的建議、非已完成實作。**版本區分（run vs. inference）**：`archicad-runtime-facts.md` 開頭驗證表列 wrapper `0.4.3` 與 `0.5.3` 皆為「observed」；issue #98 2026-08-18T16:06:58Z 貢獻者當時尚未升版，留言明講「要的話我可以在本機升到 0.5.3 重跑」；PR #123 建立於同日 18:05 UTC（換算 UTC+8 為次日 2026-08-19，與檔案頭 `Date: 2026-08-19` 相符）。0.5.3 實測的具體佐證是貢獻者本人先前的預測獲得印證：issue #98 同則留言明講「`0.5.3` 的 schema 只對齊到 Tapir 1.5.7，而我這邊的 add-on 是 1.5.8，所以 `GetDetailsOfElements` 未必會因為升版而好」，`archicad-runtime-facts.md` 記載的實測結果正是「0.5.3 + Tapir 1.5.8 → 522 個驗證錯誤」（0.4.3 為 431 個）——與該預測方向一致；`detect-clashes` 一項文件用詞是「schema 已確認」而非「已執行碰撞偵測」，本 session 判讀為讀取 `archicad_get_command_schema` 而非真的派發 `elements_get_collisions`，此區分予以保留、未在本 log 中拉平為同一種「已測試」。分頁 TTL 一項文件明文「The walk was **not** run to completion, so whether the TTL still exists is unresolved」——不明狀態原樣保留，未被本次收編改寫成肯定結論。
- **可歸屬性與去識別化檢查**：全文掃描 client／project code／案號／客戶等關鍵字無命中；`archicad-runtime-facts.md` 僅載「one solo project, metric calculation units」與既有已收編的樣板專案代稱，未含可識別客戶或案名之字串。
- **驗收（本次 session 實際跑過）**：以 `git show FETCH_HEAD:<path>` 逐檔寫入工作樹（未使用 `git checkout`／`git add`）；fidelity 以 `git diff FETCH_HEAD -- <path>`（`core.autocrlf=true`，比對前已知會有 CRLF 警告但内容比對自動忽略行尾差異）核對 3 個既有檔案皆為空 diff，新檔另以 `tr -d '\r'` 雙邊正規化後 `diff` 確認逐位元組相同；`verify-qaqc.ps1 -SkipBuild -SkipDeploy` = **64 PASS / 0 FAIL / 1 WARN / 3 SKIP**（與收編前基準一致，`Skills=54`／`Domain=79`／`Tools=180`，唯一 WARN 為既有的 `check_sanitary_fixture_requirements` 隔離項，與本次改動無關）；`git status --short --untracked-files=all` 僅列出上述 4 個意圖檔案（3 修改＋1 新增）、`git stash list` 為空、`git worktree list` 僅主 worktree；未觸發 MCP Registry 同步迴圈（`server.json`／`package.json`／schema 三處皆未變動）。
- **本 repo 未複驗聲明**：本環境無 Archicad、無法連線 Tapir MCP、無法重跑 `archicad_list_commands`／`elements_get_relations_of_elements`／`elements_get_details_of_elements` 等任何指令。本則 log 內所有 Archicad-runtime 具體數字（522／431 個驗證錯誤、7 個 Zone 中 6/1 的關係回傳、13 個 hotlink 節點、約 250 個指令、`"90°"`／`1.570796327` 雙層對照等）完全是 @Archwiz-boss 的回報，記錄時明確標示為對方證據、不併入我方驗證結果，本 log 未對任何 Archicad-runtime 行為使用「已驗證」「已測試」字樣。
- **inspector-ops 覆核後的一處刻意偏離（補於 2026-08-29，非首次收編時所寫）**：本次對貢獻者原文做了一處刻意偏離——在 `archicad-runtime-facts.md` 標題之後、既有內容之前，新增一段標示為「維護者補註（收編時新增，非貢獻者原文）」的 blockquote，明講本檔全部 runtime 觀測來自 @Archwiz-boss 在其自身環境的實測並經 PR #123 提出、本 repo 未複驗，且文中原有的「this repository」一詞在貢獻者原始 fork（`Archwiz-boss/BIM_MCP_study`）中字面為真，但跨 repo 收編後單獨閱讀該檔的人可能誤讀成「本專案驅動了 Archicad runtime」，故加註釐清。理由：inspector-ops 指出這句歸屬語意在檔案本身脫離 log 語境後會反轉，log 在檔案之外、讀該檔的人看不到。此改動使該檔與 `FETCH_HEAD` 不再逐位元組相同（僅此一檔、僅此一段新增，其餘內容未動），此偏離為預期內且已於此明講，未默默夾帶。

## [2026-08-29 01:59] docs | S12 文件對齊掃描——補 7-15／3-4 缺口、README 403 閘門、兩張 domain-index 過期卡片（純文件，計數不變）

- actor: claude-sonnet-5 (via Claude Code, Windows) — `/loop-up` implementer 角色，S12 收尾對齊階段
- files: CLAUDE.md, README.md, README.zh-TW.md, docs/BIM_MCP/reference/domain-index.html, docs/BIM_MCP/reference/tools-index.html
- trigger: manual（`/loop-up` 多代理迴圈，orchestrator 核定「把 docs/BIM_MCP 與其餘文件對齊到今晚 11 個 commit（`40432bd..5dad238`）之後的真實狀態」後由本 implementer 執行；本階段是純文件掃描，QA/QC 閘門本身已全綠，任務是找閘門看不到的散文漂移）
- summary: 逐一讀完今晚 11 個 commit 的完整 commit body 後，對 CLAUDE.md／DOCUMENT_AUDIENCE_INVENTORY.md／Key Source Files 表／domain/README.md／CLAUDE.md 觸發表／`docs/BIM_MCP/**` 做地毯式核對，找到並修正 5 處具體漂移：
  1. **CLAUDE.md QA/QC 段落漏了 `7-15`**（`360b349` 今晚新增的工具↔dispatcher 對帳閘門，只出現在腳本裡，CLAUDE.md 的 must-cover 清單完全沒提）——補一條說明其設計（正向對帳、glob 收集 dispatcher 檔案、剝註解後比對、`$knownUnimplemented` 隔離清單漂移自檢）。
  2. **`3-4` 的敘述沒跟上同一個 commit 對它的行為變更**（掃描母體為空時從假 PASS 改成 SKIP）——原有敘述只講「hit count 對不上清單長度就 FAIL」的自檢，沒提空母體這條，已補一句。
  3. **README.md／README.zh-TW.md 的「Switching Between AI Clients」／「AI Client 切換與並用限制」段落只講 HTTP 409**——這正是 `d057047` 已經修過的同一個缺口（CLAUDE.md 的 Single-Connection Limitation 補了 403 Origin 閘門），但那次修正只動了 CLAUDE.md，兩份 README 從未跟上：使用者可能會在瀏覽器帶 Origin 連線時看到 403 而不知道那是什麼，兩份文件都補了一段簡短說明（403 排在 409 鎖定檢查之前、無 settings 開關、不影響 MCP bridge）。
  4. **`docs/BIM_MCP/reference/tools-index.html` 的 `door-window-legend-tools` 卡片描述停在 `mode=migrate` 之前**——`c4a5982` 已經把 TS 原始描述改成含 A+ managed／`mode=migrate`，但生成頁沒跟著重新產生，卡片文字對讀者而言仍在講「新圖例」而非「A+ managed 圖例＋Detail Group／ExtensibleStorage ownership」，已同步成與 TS 原始描述一致的文字。
  5. **`docs/BIM_MCP/reference/domain-index.html` 兩張卡片沿用了 7 月骨架階段的描述**（`cad-block-point-placement`、`mep-opening-candidate-scan`），內含該階段對其中一位貢獻者使用的真名——該字串仍存於 append-only 的 `log/2026-07.md:164,170`，依 append-only 原則不回頭修改，本條目在此僅指涉、不重述——且對應的 `↔` 標籤都寫死 `(SOP 骨架)`。兩份 domain 檔案本身在今晚都已經走出「骨架」狀態——`cad-block-point-placement.md` 開頭有「已合併、尚未在真實 Revit 環境驗證」狀態橫幅（三工具已由 @Rumi-3653 實作合併），`mep-opening-candidate-scan.md` 開頭寫「v1 已實作；Revit 2024 runtime smoke 由貢獻者 @NicheSam 於其環境完成回報（本 repo 未複驗）」——且兩份 domain 檔的 frontmatter 現已將 NicheSam 的真名標成「待確認真名」，與 domain-index 卡片先前寫死的舊真名互相矛盾。已把兩張卡片改寫成「三工具已合併、尚未在真實 Revit 環境驗證」/「v1 已實作並合併；...本 repo 未複驗」，`↔` 標籤分別改成對應的實際工具名（`create_family_instances_from_dwg_blocks`／`scan_opening_candidates`），不再帶真名。
- **找到但刻意不動的項目（附理由）**：
  - **CLAUDE.md 的 Key Source Files 表**：沒有補 `OpeningCandidateScanner.cs`／`CadBlockPlacementExecutor.cs`／`CommandExecutor.RoomRenumber.cs`／`cad-block-placement-tools.ts`／`test-opening-candidate-contract.mjs`。核對過這張表本來就是選擇性收錄（`MCP/Core/*.cs` 現有 18 個檔案，表裡只列 4 個；`MCP-Server/src/tools/*.ts` 現有 43 個，表裡只列 3 個；`MCP-Server/scripts/` 現有數十個腳本，表裡只列 `build-apps.mjs` 因為它是必要建置步驟），既有的同類檔案（`DwgColumnExecutor.cs`／`ClashDetector.cs`／`FloorSlopeAnalyzer.cs` 等）從未被列入，補新檔會打破既有的篩選標準、不是修正漂移而是新增範圍，故不動。
  - **`docs/DOCUMENT_AUDIENCE_INVENTORY.md`**：計數（180／79／54）已是最新；新增的六個檔案（`OpeningCandidateScanner.cs`／`CadBlockPlacementExecutor.cs`／`CommandExecutor.RoomRenumber.cs`／`cad-block-placement-tools.ts`／`test-opening-candidate-contract.mjs`／`archicad-runtime-facts.md`）全部落在既有萬用字元規則之下（該檔本來就不逐檔列 C#／TS 原始碼，`.claude/skills/*/references/` 子目錄也向來不單獨列——`archicad-skill-adapter` 之外還有 3 個 skill 早就有 `references/` 子目錄、同樣沒有列），無需新增列。
  - **`domain/README.md`**：本身的 Domain↔Skill 對照表只涵蓋 38／79 個 domain 檔（20 個有對應 Skill＋18 個沒有），`door-window-legend-workflow.md` 與 `cad-block-point-placement.md` 兩者原本就不在這兩張表裡——這是遠早於今晚、涵蓋範圍本來就沒到 79 的既有缺口，不是今晚 11 個 commit 造成的，判定超出本階段範圍不動。
  - **`docs/BIM_MCP/reference/contributors.html`**：頁面明文宣稱「17 位建置協助人」（title／hero-sub／footer 三處），目前確實剛好等於檔案內 17 個不重複 GitHub handle；但今晚收編的 `NicheSam`／`Rumi-3653`／`Archwiz-boss` 三位新貢獻者完全不在這份名單裡，若按頁面自己的定義（「被收編進 main 的 fork 貢獻者名單」）理論上該數字要往上調。沒有動它：(a) footer 標注這是 `docs/fork-audit/contributor-map.md`（gitignored 內部工作檔）與 `log/2026-07.md` 驅動的稽核產物，暗示更新走獨立稽核流程而非隨手編輯；(b) 頁面自己的格式承諾是「真名 + GitHub handle」，但 `NicheSam` 的真名本身在今晚的 domain 檔裡就標成「待確認」、`Rumi-3653`／`Archwiz-boss` 則完全沒有已知真名，硬填會有捏造風險；(c) 這不在 orchestrator 列出的已知線索內，屬於自行擴大範圍。已如實記錄在此，留給維護者決定是否要跑一次正式的貢獻者稽核更新。
- **驗收（本次 session 實際跑過）**：`verify-qaqc.ps1 -SkipBuild -SkipDeploy` 在每次編輯後都重跑過一次，最終 = **64 PASS / 0 FAIL / 1 WARN / 3 SKIP**（與收編前基準一致，唯一 WARN 為既有的 `check_sanitary_fixture_requirements` 隔離項，與本次改動無關；`7-0` 計數 Skills=54／Domain=79／Tools=180 不變）；`git diff --stat` = 5 檔、11 insertions(+)／6 deletions(-)；`git status --short --untracked-files=all` 僅列出上述 5 個意圖修改檔案、`git stash list` 為空、`git worktree list` 僅主 worktree；未執行任何 git 寫入指令（無 commit／push／add）；未觸碰 `vault/`、`MCP/`、`MCP-Server/src/`、`scripts/verify-qaqc.ps1`。
- **誠實邊界**：本次未在任何真實 Revit 或 Archicad 環境執行任何工具；所有編輯只是把散文敘述對齊到 domain 檔案／TS 原始碼／腳本本身已經記錄的狀態，沒有把任何「未驗證」升級成「已驗證」的字樣，`cad-block-point-placement`／`mep-opening-candidate-scan` 兩張卡片改寫後仍明確保留「尚未在真實 Revit 環境驗證」／「本 repo 未複驗」的措辭。
