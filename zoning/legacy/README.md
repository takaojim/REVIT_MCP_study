# 歷史資料

`unclassified/` 保留原先 zoning 根目錄中無法可靠確認所屬計畫的 Revit 快照／空資料，不猜測歸屬、不刪除。由製作者後續確認模型與來源再分類；檢核者不得讀取。

`authoring/` 保存原 `.claude` 與 `.agents` 的兩份製作技能。兩者對行距、分頁等規定不一致，僅供製作者追溯，不能把歷史常數套用所有計畫，也不能執行其未經整理的舊腳本。現行入口是 `zoning/skills/zoning-legend-author/SKILL.md`。

`migration.json` 記錄搬移前後路徑與搬移當時的 SHA256。之後補寫交接連結等文件可能有新 hash；此表不是目前檔案完整性簽章。原始 PDF 與資料搬移時均核對內容未變。
